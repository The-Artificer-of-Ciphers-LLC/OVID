---
quick_id: 260707-u7v
slug: staging-deploy-doc-sync
status: planned
date: 2026-07-08
---

# Quick Task: Sync docs/deployment.md Staging section with live D-06 deploy

## Objective

The D-06 staging deploy is already LIVE and verified on infrastructure that diverged from what
`docs/deployment.md`'s Staging section describes. This is a documentation-only sync — no
infrastructure changes. Update the Staging section to match reality.

## Tasks

1. Rename the API host `api.staging.oviddb.org` → `staging-api.oviddb.org` everywhere in the
   Staging section (header, host/port table, prerequisites, env-wiring, compose recipe). Add a
   one-line rationale: Cloudflare's free Universal SSL edge cert (`oviddb.org` + `*.oviddb.org`)
   only covers one wildcard label, so a single-label API host is required for coverage.
2. Correct ports/slot: staging reuses the retired `test` (x200) slot on holodeck — not a new x300
   bracket. Web → `holodeck:3200` → container `:3000`; API → `holodeck:8200` → container `:8000`.
3. Correct TLS/cert instructions: no SAN cert or new origin cert needed (both staging hostnames
   are single-label, covered by the `*.oviddb.org` edge cert); origin reuses the existing
   `oviddb.org` LE cert under Cloudflare **Full** (not strict) mode. Update nginx vhost
   description accordingly. Add `ssl_stapling` removal note (OCSP deprecated 2025).
4. Correct DNS instructions: proxied CNAME records → `oviddb.org`, not A records to
   `64.98.89.233`.
5. Correct the `docker-compose.staging.yml` example and build/run commands: ports 3300→3200,
   8300→8200; web build arg `NEXT_PUBLIC_API_URL` → `https://staging-api.oviddb.org`.
6. Verify no stale `api.staging.oviddb.org` / `:3300` / `:8300` references remain in the Staging
   section; commit.

## Scope

Only `docs/deployment.md`'s Staging section (`## Staging (...)` through the "Human sign-off"
subsection). No infrastructure, no other doc sections.
