---
phase: 03-redis-backed-rate-limiting-performance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - api/requirements.txt
  - api/app/rate_limit.py
  - api/tests/test_rate_limit_backend.py
  - api/tests/test_rate_limit_fallback.py
  - api/tests/test_startup_guard.py
autonomous: true
requirements: [INFRA-01, INFRA-02]
user_setup: []

must_haves:
  truths:
    - "With REDIS_URL set, the slowapi Limiter uses a shared RedisStorage backend; with REDIS_URL unset it uses MemoryStorage (single-worker-correct, today's behavior) — per D-05a."
    - "During a Redis outage (RedisStorage.incr raising ConnectionError), requests keep returning 200 within FALLBACK_LIMIT per worker and 429 once that per-worker cap is exceeded — bounded fail-open, never fail-closed on the read path (D-01/D-02)."
    - "The process refuses to boot (RuntimeError at import) when OVID_WORKERS>1 (or WEB_CONCURRENCY>1) and REDIS_URL is unset — turning silent Nx inflation into a loud failure (D-06)."
  artifacts:
    - "api/app/rate_limit.py — env-driven storage_uri, in-memory fallback flags, AUTH_WRITE_LIMIT and FALLBACK_LIMIT constants, module-level fail-fast guard"
    - "api/requirements.txt — redis>=5,<8 pin"
    - "api/tests/test_rate_limit_backend.py, api/tests/test_rate_limit_fallback.py, api/tests/test_startup_guard.py"
  key_links:
    - "rate_limit.py reads REDIS_URL once at import → sets storage_uri and gates swallow_errors/in_memory_fallback_enabled/in_memory_fallback on bool(REDIS_URL)"
    - "the D-06 guard reads OVID_WORKERS (fallback WEB_CONCURRENCY) and raises when >1 without REDIS_URL"
    - "the existing structured-JSON 429 handler (rate_limit.py lines 72-96) stays unchanged and is reused for any new output"
---

<objective>
Rework `api/app/rate_limit.py` so the general slowapi limiter is a **shared, cross-worker Redis store when configured, a bounded self-healing fallback during a Redis outage, and a fail-fast boot guard against silent multi-worker inflation** — the coherent backbone locked by D-00. This closes the INFRA-01 multi-worker correctness defect (the limiter's own docstring documents the Nx inflation) and delivers the INFRA-02 tested outage decision (D-01 in-memory fallback).

Purpose: Every p95 claim (INFRA-03) and every write throttle (INFRA-04) is hollow under `memory://` because counters are per-worker; this plan makes Redis the shared store first, so the rest of the phase validates the real target config.

Output: env-driven `rate_limit.py` (backend selection + fallback + guard + two tunable constants), the `redis>=5,<8` dependency pin, and three new unit tests pinning each behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-redis-backed-rate-limiting-performance/03-CONTEXT.md
@.planning/phases/03-redis-backed-rate-limiting-performance/03-RESEARCH.md
@.planning/phases/03-redis-backed-rate-limiting-performance/03-PATTERNS.md
@api/app/rate_limit.py
@api/app/auth/config.py
@api/tests/conftest.py
@api/tests/test_rate_limit.py
</context>

<artifacts>
## Artifacts this plan produces
- **New constants** in `api/app/rate_limit.py`: `AUTH_WRITE_LIMIT = "20/minute;300/hour"` (D-08, consumed by Plan 02), `FALLBACK_LIMIT = "60/minute"` (D-01 conservative per-worker cap, Claude's discretion per RESEARCH A2).
- **New env vars consumed at import:** `REDIS_URL` (backend selection), `OVID_WORKERS` (fallback `WEB_CONCURRENCY`, default `1`) — the D-06 guard input.
- **New module-level guard function/statement** in `rate_limit.py` raising `RuntimeError` on multi-worker + `memory://` misconfiguration.
- **New dependency:** `redis>=5,<8` in `api/requirements.txt`.
- **New tests:** `test_rate_limit_backend.py`, `test_rate_limit_fallback.py`, `test_startup_guard.py`.
- Note: `api/main.py` needs **no change** — it imports from `rate_limit` at module load (line 14), so the import-time guard fires automatically. Confirm, do not invent a `main.py` edit (PATTERNS discrepancy #2).
</context>

<tasks>

<task type="execute">
  <name>Task 1: Pin and install the redis client dependency</name>
  <files>api/requirements.txt</files>
  <read_first>
    - `api/requirements.txt` — match the existing `>=X,<Y` range-pin style used by the other deps.
    - `.planning/phases/03-redis-backed-rate-limiting-performance/03-RESEARCH.md` §"Package Legitimacy Audit" (redis = OK/Approved, first-party `redis/redis-py`) and §Pitfall 5.
  </read_first>
  <action>
    Add a single line `redis>=5,<8` to `api/requirements.txt`. The upper bound is mandatory: `limits 5.8.0` (slowapi's storage engine) constrains its redis extra to `<8.0.0`, and an unpinned `redis` resolves to 8.0.1, outside that supported range (D-05a groundwork; RESEARCH Pitfall 5). Do NOT add `redis` to `loadtest/requirements.txt` — that is Plan 04's isolated harness dep. Install into the API virtualenv so `limits.storage.RedisStorage` becomes constructable for the tests in Tasks 2-3. Before finalizing the pin, confirm the resolvable range with the venv pip (`.venv/bin/pip index versions redis`) since in-sandbox registry lookups were network-blocked during research (RESEARCH A3) — keep `>=5,<8` unless pip proves a tighter bound is required. This package is verdict OK in the completed legitimacy audit (pure-Python wheel, no postinstall script), so no blocking-human legitimacy checkpoint is required.
  </action>
  <verify>
    <automated>cd api && .venv/bin/python -c "import redis, limits.storage; from importlib.metadata import version; v=version('redis'); assert tuple(int(x) for x in v.split('.')[:1]) < (8,), v; print('redis', v, 'RedisStorage', limits.storage.RedisStorage)"</automated>
  </verify>
  <done>`redis>=5,<8` is pinned in requirements and importable in the API venv at a version < 8.0.0; `limits.storage.RedisStorage` is constructable.</done>
  <acceptance_criteria>
    - `api/requirements.txt` contains `redis>=5,<8`.
    - `redis` imports in the API venv at a version `< 8.0.0`, and `limits.storage.RedisStorage` is importable.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Env-driven Redis backend selection with in-memory outage fallback (INFRA-01 + INFRA-02)</name>
  <files>api/app/rate_limit.py, api/tests/test_rate_limit_backend.py, api/tests/test_rate_limit_fallback.py</files>
  <read_first>
    - `api/app/rate_limit.py` lines 22-23 (existing `UNAUTH_LIMIT`/`AUTH_LIMIT` constant block), lines 65-69 (`Limiter(...)` construction to modify), lines 72-96 (structured-JSON 429 handler — leave unchanged, reuse its envelope), lines 26-46 (`_auth_aware_key`).
    - `.planning/phases/03-redis-backed-rate-limiting-performance/03-RESEARCH.md` §"Pattern 1", §Pitfall 1 (fallback replaces ALL per-route limits globally), §Pitfall 3 & 4 (test must build its OWN redis-configured limiter; patch `RedisStorage.incr`, the method `FixedWindowRateLimiter.hit()` actually calls — not `.get`).
    - `api/tests/conftest.py` lines 93-104 (autouse `_reset_rate_limiter` resets the APP limiter — the fallback test's local limiter is separate and unaffected) and the `DATABASE_URL=sqlite` / no-`REDIS_URL` test env.
    - `api/tests/test_rate_limit.py` for the existing test shape.
  </read_first>
  <behavior>
    - Backend selection: `REDIS_URL` set → the limiter's active storage is a `limits` `RedisStorage`; `REDIS_URL` unset → `MemoryStorage` (today's single-worker-correct default preserved).
    - Fallback (INFRA-02, D-03): with a redis-configured limiter whose `RedisStorage.incr` raises `redis.exceptions.ConnectionError`, requests return 200 while the fallback counter is under `FALLBACK_LIMIT`, then 429 once the per-worker fallback cap is exceeded; the storage method is restored in `finally`.
    - `FALLBACK_LIMIT` is a single GLOBAL per-worker cap that replaces every per-route limit during an outage (Pitfall 1) — so its value must also protect the read path.
  </behavior>
  <action>
    RED first. Write `test_rate_limit_backend.py`: assert backend selection deterministically and in isolation from module-global pollution — spawn a subprocess (`.venv/bin/python -c ...`) with `REDIS_URL` set that imports `app.rate_limit` and prints the active storage class name, asserting it is `RedisStorage`; a second subprocess with `REDIS_URL` unset asserting `MemoryStorage`. Introspect the limiter's storage via the attribute slowapi 0.1.10 exposes (RESEARCH cites the private storage handle `_storage`; confirm against the installed source and use the correct name). Prefer subprocess isolation over `importlib.reload` because the app limiter is a module global; if you use `reload`, restore the module to the `REDIS_URL`-unset state in `finally`.

    Write `test_rate_limit_fallback.py` following the repo's cross-platform IO-failure convention (save original → override to raise → assert → restore in `finally`, per CLAUDE.md): construct a LOCAL `Limiter(storage_uri="redis://localhost:6379", swallow_errors=True, in_memory_fallback_enabled=True, in_memory_fallback=[a small cap like "3/minute"])`, mount it on a minimal FastAPI app with one decorated route and the existing `rate_limit_exceeded_handler`, wrap in `TestClient`. Monkeypatch `limits.storage.RedisStorage.incr` to raise `redis.exceptions.ConnectionError`. Drive requests: assert 200 while under the small cap, then 429 once exceeded. Restore `incr` in `finally`. No live Redis is needed — `redis.from_url` does not connect eagerly (RESEARCH Pitfall 3/4). Run both tests; they MUST fail before the implementation change.

    GREEN. Edit the `Limiter(...)` construction (lines 65-69) to read `REDIS_URL` from the environment once at module level: set `storage_uri` to the redis URL when present else `memory://`; add `swallow_errors=bool(redis_url)`, `in_memory_fallback_enabled=bool(redis_url)`, and `in_memory_fallback=[FALLBACK_LIMIT] if redis_url else []`. Add `AUTH_WRITE_LIMIT = "20/minute;300/hour"` (D-08 — consumed by Plan 02) and `FALLBACK_LIMIT = "60/minute"` (D-01 conservative per-worker cap; ~240/min aggregate across 4 workers during an outage — protects the read path per Pitfall 1) to the `UPPER_SNAKE_CASE` constant block near lines 22-23, each with a one-line comment marking it a tunable launch-safe default (never a magic number). Leave `UNAUTH_LIMIT`, `AUTH_LIMIT`, `_auth_aware_key`, `_dynamic_limit`, and the 429 handler untouched. Do NOT write any custom storage/fallback code — every flag is a native slowapi 0.1.10 feature (verified in RESEARCH). Re-run; tests MUST pass.
  </action>
  <verify>
    <automated>cd api && .venv/bin/python -m pytest tests/test_rate_limit_backend.py tests/test_rate_limit_fallback.py -x</automated>
  </verify>
  <done>Backend selection and outage fallback are proven by passing tests; `AUTH_WRITE_LIMIT`/`FALLBACK_LIMIT` constants exist; fallback flags gate on `bool(REDIS_URL)`; no custom storage code; 429 handler and read limits unchanged.</done>
  <acceptance_criteria>
    - `test_rate_limit_backend.py` proves RedisStorage-when-set / MemoryStorage-when-unset in process isolation.
    - `test_rate_limit_fallback.py` proves 200-within-cap then 429 under injected `ConnectionError`, with the patched method restored in `finally`.
    - `AUTH_WRITE_LIMIT` and `FALLBACK_LIMIT` exist as named constants; the fallback flags are gated on `bool(REDIS_URL)` so the unset default stays pure `memory://`.
    - No custom storage code added; the 429 handler and read-tier limits are unchanged.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Fail-fast startup guard against multi-worker memory:// (D-06)</name>
  <files>api/app/rate_limit.py, api/tests/test_startup_guard.py</files>
  <read_first>
    - `api/app/auth/config.py` lines 6-18 (`_require_env` → module-level `raise RuntimeError` at import — the exact fail-fast convention to mirror).
    - `api/app/rate_limit.py` — the module-level `REDIS_URL` read added in Task 2 (the guard reads the same variable).
    - `.planning/phases/03-redis-backed-rate-limiting-performance/03-RESEARCH.md` §"Pattern 2" and §Anti-Patterns (reject argv scraping; use an explicit env var).
  </read_first>
  <behavior>
    - `OVID_WORKERS>1` (or `WEB_CONCURRENCY>1`) with `REDIS_URL` unset → importing `app.rate_limit` raises `RuntimeError` with a message naming the worker count and the fix (set `REDIS_URL` or run 1 worker).
    - `OVID_WORKERS>1` WITH `REDIS_URL` set → imports cleanly (no raise).
    - `OVID_WORKERS` unset / `=1` → imports cleanly regardless of `REDIS_URL` (self-host single-worker path stays frictionless, D-05a).
  </behavior>
  <action>
    RED first. Write `test_startup_guard.py` using subprocess isolation (import-time behavior cannot be safely re-triggered on the module global otherwise): spawn `.venv/bin/python -c "import app.rate_limit"` with a controlled environment and assert exit codes / stderr — (a) `OVID_WORKERS=2` + no `REDIS_URL` → non-zero exit and a `RuntimeError` mentioning the guard; (b) `OVID_WORKERS=2` + `REDIS_URL=redis://x` → clean exit 0; (c) `OVID_WORKERS` unset → clean exit 0. Run; the guard-raise case MUST currently fail (no guard exists yet).

    GREEN. Add a module-level guard in `rate_limit.py` immediately after the `REDIS_URL` read: parse `int(os.environ.get("OVID_WORKERS", os.environ.get("WEB_CONCURRENCY", "1")))` and `raise RuntimeError(...)` when that value `> 1` and `REDIS_URL` is unset. The message must explain that `memory://` gives each worker an independent counter (Nx rate-limit inflation) and instruct to set `REDIS_URL` or run one worker — mirroring `auth/config.py::_require_env`'s clear fail-fast message. Do NOT parse gunicorn argv (D-06 rejects fragile scraping). Confirm `api/main.py` needs no edit: it imports from `rate_limit` (line 14) so the guard fires on that import automatically (PATTERNS discrepancy #2) — leave `main.py` out of `files_modified`. Re-run; all three cases pass.
  </action>
  <verify>
    <automated>cd api && .venv/bin/python -m pytest tests/test_startup_guard.py -x</automated>
  </verify>
  <done>The import-time guard raises on multi-worker + `memory://`, boots clean otherwise, uses an explicit env var (never argv), and `api/main.py` is unmodified.</done>
  <acceptance_criteria>
    - Boot raises `RuntimeError` for `OVID_WORKERS>1` + unset `REDIS_URL`; boots clean when `REDIS_URL` is set or workers ≤ 1.
    - The guard uses an explicit env var (`OVID_WORKERS`, fallback `WEB_CONCURRENCY`), never argv parsing.
    - `api/main.py` is unmodified; the guard fires via the existing `rate_limit` import.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gunicorn worker → rate-limit store | Each worker's limit check crosses into shared (Redis) or per-worker (memory) counter state; correctness of the abuse-prevention control depends on which |
| deploy config (env) → API bootstrap | `REDIS_URL` / `OVID_WORKERS` are operator-supplied; a misconfiguration silently weakens the control |
| pip index → API image | New `redis` wheel enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / DoS bypass | multi-worker `memory://` counters (the current defect) | high | mitigate | Redis shared store when `REDIS_URL` set (INFRA-01) + D-06 fail-fast guard that refuses to boot on `OVID_WORKERS>1` without `REDIS_URL` — turns silent Nx inflation into a loud failure |
| T-03-02 | Denial of Service | Redis as new availability single-point during an outage | medium | mitigate | Bounded `FALLBACK_LIMIT` per worker (D-01) with auto-recovery; never fail-closed on the read-heavy ARM lookup path (D-02); cap chosen to still protect reads (Pitfall 1) |
| T-03-SC | Tampering | pip install of `redis` | high | mitigate | Package Legitimacy Audit complete (RESEARCH — `redis` verdict OK, first-party `redis/redis-py`, pure-Python wheel, no postinstall); pinned `redis>=5,<8`; no `[ASSUMED]`/`[SUS]` package, so no blocking-human checkpoint required |
</threat_model>

<verification>
- `cd api && .venv/bin/python -m pytest tests/test_rate_limit_backend.py tests/test_rate_limit_fallback.py tests/test_startup_guard.py -x` all green.
- Regression: `cd api && .venv/bin/python -m pytest tests/test_rate_limit.py -q` still passes unchanged — the env-driven default keeps `memory://` in the test env (no `REDIS_URL`, `OVID_WORKERS` unset), so existing behavior is preserved (RESEARCH Wave 0 regression item).
- Full suite: `cd api && .venv/bin/python -m pytest tests/ -q` green.
</verification>

<success_criteria>
- INFRA-01 code path: `REDIS_URL` selects a shared `RedisStorage`; unset preserves `memory://`; multi-worker + unset refuses to boot.
- INFRA-02 tested decision: injected `ConnectionError` degrades to a bounded per-worker fallback (200 then 429), restored in `finally` — the D-01/D-03 decision proven by test.
- Two tunable named constants (`AUTH_WRITE_LIMIT`, `FALLBACK_LIMIT`) exist for Plan 02 and outage tuning.
- Existing rate-limit tests remain green; no `main.py` change.
</success_criteria>

<output>
Create `.planning/phases/03-redis-backed-rate-limiting-performance/03-01-SUMMARY.md` when done.
</output>
