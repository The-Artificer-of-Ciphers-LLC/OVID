---
phase: 05-adr-0001-completion-dvdread1-promotion
plan: 06
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - api/app/migrations_support.py
  - api/tests/test_fingerprint_registry_migration.py
  - api/alembic/versions/900000000005_add_fingerprint_registry.py
  - api/alembic/versions/900000000006_promote_dvdread1_primary.py
autonomous: true
requirements: [IDENT-04]

must_haves:
  truths:
    - "The fingerprint_registry table is created and backfilled from BOTH discs.fingerprint and disc_identity_aliases.fingerprint inside a single migration transaction — no window where the registry lags reality (D-02)"
    - "The registry create+backfill migration (revision 900000000005) is ordered strictly BEFORE the promotion migration (revision 900000000006) in the Alembic revision chain, per the phase's locked sequencing"
    - "Running alembic upgrade head promotes every disc that has a recorded dvdread1-* alias to dvdread1-* primary, each in its own committed transaction; any disc without a dvdread1-* alias is left untouched on dvd1-* (D-01)"
    - "The pre-existing IDENT-05 guardrail (api/tests/test_disc_identity_regression.py) continues to pass completely unmodified after this plan lands — it asserts identity/structure via DB lookup by the fixed dvd1-* string, not the literal top-level fingerprint value, so promotion does not break it (Pitfall 4, D-16)"
  artifacts:
    - "api/alembic/versions/900000000005_add_fingerprint_registry.py"
    - "api/alembic/versions/900000000006_promote_dvdread1_primary.py"
    - "backfill_fingerprint_registry() in api/app/migrations_support.py"
    - "api/tests/test_fingerprint_registry_migration.py"
  key_links:
    - "900000000006's upgrade() imports and calls promote_all_dvdread1_discs() from Plan 05-02's migrations_support.py, with a connection.commit() per disc"
    - "900000000005's fingerprint_registry DDL must match Plan 05-01's FingerprintRegistry ORM model exactly (String(50) unique fingerprint, UUID disc_id FK, disc_id index)"
---

<objective>
Land the two Alembic migrations that complete ADR 0001 Phase 3: revision `900000000005` creates the `fingerprint_registry` table and backfills it from both existing tables (D-02), chained strictly before revision `900000000006`, which performs the one-time `dvd1-* -> dvdread1-*` promotion backfill (D-01) by wrapping Plan 05-02's already-tested `promote_all_dvdread1_discs()`.

Purpose: this is where the previously-independent, unit-tested pieces (the `FingerprintRegistry` model from Plan 05-01, and `promote_one_disc`/`promote_all_dvdread1_discs` from Plan 05-02) become real, runnable schema changes — sequenced so the cross-table arbitration backstop exists before the promotion migration that increases write concurrency on the shared fingerprint namespace runs.
Output: two new Alembic migration files chained onto the existing head (`900000000004`), a small `backfill_fingerprint_registry()` helper, and proof that the pre-existing IDENT-05 regression test survives unmodified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-CONTEXT.md
@.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-PATTERNS.md
@.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-01-PLAN.md
@.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backfill_fingerprint_registry() testable helper</name>
  <files>api/app/migrations_support.py, api/tests/test_fingerprint_registry_migration.py</files>
  <read_first>
    api/app/migrations_support.py (Plan 05-02's `promote_one_disc`/`promote_all_dvdread1_discs`, for module style consistency — this task adds a third function to the same file)
    api/app/models.py `FingerprintRegistry` (Plan 05-01 — the schema this backfill populates)
    api/alembic/versions/900000000003_add_disc_identity_aliases.py (full file — the table-creation style analog Task 2 mirrors)
    .planning/phases/05-adr-0001-completion-dvdread1-promotion/05-PATTERNS.md section "api/alembic/versions/&lt;rev-A&gt;_add_fingerprint_registry.py (migration, batch)"
  </read_first>
  <behavior>
    - Given N discs and M `disc_identity_aliases` rows already in the DB (zero `fingerprint_registry` rows yet), `backfill_fingerprint_registry(connection)` returns `(N, M)` — the counts inserted from each source table — and afterward `fingerprint_registry` has exactly `N+M` rows total.
    - The function is dialect-portable: it generates each new registry row's UUID in Python (`uuid.uuid4()`) rather than relying on a database-side UUID-generation function, so it is directly exercised (and asserted) against the in-memory SQLite test harness — not skipped/mocked.
  </behavior>
  <action>
    Add `backfill_fingerprint_registry(connection: Connection) -> tuple[int, int]` to `api/app/migrations_support.py`. This is a one-time, non-per-row-idempotent bulk backfill (per Pitfall 2 — unlike `promote_one_disc`, it needs no `WHERE`-guard resumability, since Alembic's own revision tracking guarantees `upgrade()` runs exactly once): read all `(id, fingerprint)` rows from `discs` and all `(disc_id, fingerprint)` rows from `disc_identity_aliases` via two `SELECT`s, then insert one new `fingerprint_registry` row per source row using parameterized `INSERT` statements with a freshly generated `uuid.uuid4()` id and `datetime.now(timezone.utc)` for `created_at` for each (looping in Python rather than a database-side `INSERT ... SELECT` with a UUID-generation function, so the exact same code path runs identically against SQLite and PostgreSQL). Return `(count_from_discs, count_from_aliases)`. Add both behaviors above as tests in `api/tests/test_fingerprint_registry_migration.py`.
  </action>
  <acceptance_criteria>
    `grep -c "def backfill_fingerprint_registry" api/app/migrations_support.py` returns 1.
    Both tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd api && python -m pytest tests/test_fingerprint_registry_migration.py -v</automated>
  </verify>
  <done>backfill_fingerprint_registry() exists, is dialect-portable (no Postgres-only UUID generation), and is proven correct against the in-memory SQLite harness.</done>
</task>

<task type="auto">
  <name>Task 2: rev-A Alembic migration — fingerprint_registry create + backfill</name>
  <files>api/alembic/versions/900000000005_add_fingerprint_registry.py</files>
  <read_first>
    api/alembic/versions/900000000003_add_disc_identity_aliases.py (full file — the exact revision-header/create_table/create_index/downgrade template to follow)
    api/app/models.py `FingerprintRegistry` (Plan 05-01 — match this schema exactly)
    api/app/migrations_support.py `backfill_fingerprint_registry` (Task 1, just written)
  </read_first>
  <action>
    Create `api/alembic/versions/900000000005_add_fingerprint_registry.py` following `900000000003`'s exact structure: `revision: str = '900000000005'`, `down_revision: Union[str, None] = '900000000004'` (the current head). `upgrade()`: `op.create_table('fingerprint_registry', sa.Column('id', postgresql.UUID(as_uuid=True), nullable=False), sa.Column('fingerprint', sa.String(length=50), nullable=False), sa.Column('disc_id', postgresql.UUID(as_uuid=True), nullable=False), sa.Column('created_at', sa.DateTime(timezone=True), nullable=True), sa.ForeignKeyConstraint(['disc_id'], ['discs.id']), sa.PrimaryKeyConstraint('id'), sa.UniqueConstraint('fingerprint'))`, then `op.create_index('idx_fingerprint_registry_disc_id', 'fingerprint_registry', ['disc_id'])`, then `from app.migrations_support import backfill_fingerprint_registry` and call `backfill_fingerprint_registry(op.get_bind())` so the registry is populated from both existing tables inside this SAME migration transaction. `downgrade()`: `op.drop_index('idx_fingerprint_registry_disc_id', table_name='fingerprint_registry')` then `op.drop_table('fingerprint_registry')`, mirroring `900000000003`'s downgrade shape exactly.
  </action>
  <acceptance_criteria>
    `grep -c "revision: str = '900000000005'" api/alembic/versions/900000000005_add_fingerprint_registry.py` returns 1.
    `grep -c "down_revision.*900000000004" api/alembic/versions/900000000005_add_fingerprint_registry.py` returns 1.
    `grep -c "backfill_fingerprint_registry" api/alembic/versions/900000000005_add_fingerprint_registry.py` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>cd api && python -c "import ast; ast.parse(open('alembic/versions/900000000005_add_fingerprint_registry.py').read())" &amp;&amp; echo "syntax OK"</automated>
  </verify>
  <done>The rev-A migration file exists, is chained after 900000000004, creates fingerprint_registry matching the ORM model exactly, and backfills it via the Task 1 helper in the same transaction.</done>
</task>

<task type="auto">
  <name>Task 3: rev-B Alembic migration — dvdread1 promotion</name>
  <files>api/alembic/versions/900000000006_promote_dvdread1_primary.py</files>
  <read_first>
    api/app/migrations_support.py `promote_all_dvdread1_discs` (Plan 05-02 — the exact function this migration wraps)
    05-RESEARCH.md Code Example 1 (lines 483-519 — the Alembic upgrade()/downgrade() sketch this task follows, adapted to call promote_all_dvdread1_discs instead of inlining the loop)
    api/tests/test_disc_identity_regression.py (full file — confirm this task changes NOTHING here; the IDENT-05 guardrail must survive unmodified per Pitfall 4/D-16)
  </read_first>
  <action>
    Create `api/alembic/versions/900000000006_promote_dvdread1_primary.py`: `revision: str = '900000000006'`, `down_revision: Union[str, None] = '900000000005'` (chains after rev-A per the phase's locked sequencing). `upgrade()`: `connection = op.get_bind()`; `from app.migrations_support import promote_all_dvdread1_discs`; `promoted_count = promote_all_dvdread1_discs(connection)`; `print(f"Promotion complete: {promoted_count} discs promoted to dvdread1-* primary")`. `downgrade()`: an explicit documented no-op — a single `pass` statement with a comment explaining that reversing a promotion en masse is not a sanctioned operation per D-03's immutability guarantee (a downgrade here would itself violate the anti-fragmentation guarantee ADR 0001 protects) — never raise `NotImplementedError`. Do NOT modify `api/tests/test_disc_identity_regression.py` in this task.
  </action>
  <acceptance_criteria>
    `grep -c "down_revision.*900000000005" api/alembic/versions/900000000006_promote_dvdread1_primary.py` returns 1.
    `grep -c "def downgrade" api/alembic/versions/900000000006_promote_dvdread1_primary.py` returns 1.
    `git diff --stat api/tests/test_disc_identity_regression.py` shows zero changes (or the file is untouched by this plan's git history).
  </acceptance_criteria>
  <verify>
    <automated>cd api && python -m pytest tests/test_disc_identity_regression.py tests/test_promote_dvdread1_migration.py tests/test_fingerprint_registry.py tests/test_fingerprint_registry_migration.py -q</automated>
  </verify>
  <done>The rev-B migration file exists, is chained after 900000000005, wraps promote_all_dvdread1_discs() with a documented no-op downgrade, and the pre-existing IDENT-05 regression test passes completely unmodified.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `api/alembic/versions/900000000005_add_fingerprint_registry.py` — creates `fingerprint_registry`, backfills from `discs` + `disc_identity_aliases`
- `api/alembic/versions/900000000006_promote_dvdread1_primary.py` — wraps `promote_all_dvdread1_discs()`
- `backfill_fingerprint_registry()` — new function in `api/app/migrations_support.py`
- `api/tests/test_fingerprint_registry_migration.py` (new file)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-triggered `alembic upgrade head` only — no external/network input | Both migrations operate solely on the deployment's own existing DB state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Tampering | Alembic migration chain ordering | high | mitigate | `900000000005` (registry) is chained strictly before `900000000006` (promotion) via `down_revision` — matches the phase's locked sequencing requirement; a wrong chain order would leave promotion running before the cross-table arbitration backstop exists. |
| T-05-14 | Repudiation | idempotent migration re-run safety | high | mitigate | Alembic's own revision-tracking table prevents re-running an already-applied migration in the normal case; rev-B's underlying `promote_one_disc()` is ALSO independently idempotent (Plan 05-02) as defense-in-depth against a manual/forced re-run (e.g. a hand-edited `alembic_version` row). |
| T-05-15 | Denial of Service | rev-B's per-disc commit loop against a real Postgres instance | medium | mitigate | Per-disc commits (not one giant transaction) mean an interrupted run leaves already-processed discs durably promoted rather than losing all progress; proven by Plan 05-02's idempotent-re-run test, and the D-04 write-quiesce window ensures no concurrent live traffic contends with the migration while it runs. |
</threat_model>

<verification>
Run `cd api && python -m pytest tests/test_disc_identity_regression.py tests/test_promote_dvdread1_migration.py tests/test_fingerprint_registry.py tests/test_fingerprint_registry_migration.py -q` — all green. As a manual (non-automated, per 05-VALIDATION.md's Manual-Only Verifications) sanity check before the real production cutover, dry-run `alembic upgrade head` against a scratch Postgres instance (e.g. `docker compose run --rm api alembic upgrade head` against a throwaway `db` service) since CI never exercises real Alembic execution (Pitfall 5).
</verification>

<success_criteria>
Both migrations exist, correctly chained (registry before promotion); the registry is backfilled from both source tables in one transaction; the promotion migration wraps the already-tested promote_all_dvdread1_discs(); the pre-existing IDENT-05 guardrail passes unmodified.
</success_criteria>

<output>
Create `.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-06-SUMMARY.md` when done
</output>
