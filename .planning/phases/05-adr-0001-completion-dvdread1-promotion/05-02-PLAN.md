---
phase: 05-adr-0001-completion-dvdread1-promotion
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - api/app/migrations_support.py
  - api/tests/test_promote_dvdread1_migration.py
autonomous: true
requirements: [IDENT-04]

must_haves:
  truths:
    - "promote_one_disc() is idempotent: promoting an already-promoted disc, or a disc with no dvdread1-* alias, is a safe no-op returning False — never raises, never double-promotes (D-01)"
    - "Promoting a disc deletes its dvdread1-* alias row, sets discs.fingerprint to that dvdread1-* value, and inserts the OLD dvd1-* value as a new alias row — the new alias row's created_at sorts after any pre-existing aliases, preserving D-06 alias ordering (primary-first by created_at, id)"
    - "promote_all_dvdread1_discs() commits per-disc (SQLAlchemy 2.0 commit-as-you-go), so an interrupted run leaves already-processed discs durably promoted and the whole pass is safely resumable by re-running from scratch"
  artifacts:
    - "api/app/migrations_support.py (promote_one_disc, promote_all_dvdread1_discs)"
    - "api/tests/test_promote_dvdread1_migration.py"
  key_links:
    - "promote_one_disc() is imported directly by the rev-B Alembic migration (Plan 05-06) — Alembic-independent per Pitfall 5, testable via the existing in-memory SQLite pytest harness with zero Alembic invocation"
---

<objective>
Implement the D-01 per-disc promotion logic (`promote_one_disc`) and its bulk driver (`promote_all_dvdread1_discs`) as plain, Alembic-independent functions in a new `api/app/migrations_support.py` module, so the actual promotion transform is directly pytest-testable against the project's in-memory SQLite harness — CI never runs a real `alembic upgrade head` against Postgres (Pitfall 5), so this logic must be testable without it.

Purpose: this is the D-01 backfill mechanism itself — the loop that rewrites `discs.fingerprint` from `dvd1-*` to `dvdread1-*` for every disc that already has a recorded `dvdread1-*` alias, one disc per committed transaction, idempotent and resumable.
Output: `promote_one_disc()` and `promote_all_dvdread1_discs()` in `api/app/migrations_support.py`, fully covered by unit tests, ready to be wrapped by Plan 05-06's Alembic migration file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-CONTEXT.md
@.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN — promote_one_disc() per-disc promotion + idempotency</name>
  <files>api/app/migrations_support.py, api/tests/test_promote_dvdread1_migration.py</files>
  <read_first>
    .planning/phases/05-adr-0001-completion-dvdread1-promotion/05-RESEARCH.md Code Example 1 (lines 434-481 — the exact `promote_one_disc` function this task implements; do NOT implement the Alembic `upgrade()`/`downgrade()` wrapper shown alongside it, that belongs to Plan 05-06)
    api/tests/conftest.py `db_session` fixture and `seed_test_disc` pattern (for building test fixtures: real ORM `Disc`/`DiscIdentityAlias` rows, then operating on `db_session.connection()`)
    api/app/models.py `Disc`/`DiscIdentityAlias` schema (columns this raw-SQL function reads/writes)
  </read_first>
  <behavior>
    - A disc has a `dvdread1-*` alias and its current `discs.fingerprint` equals the `dvd1-*` value passed in → `promote_one_disc()` returns `True`; afterward `discs.fingerprint` equals the `dvdread1-*` value; the OLD `dvd1-*` value now exists as a `disc_identity_aliases` row for that disc; the `dvdread1-*` alias row is gone (deleted, not duplicated).
    - A disc's `discs.fingerprint` no longer equals the `dvd1-*` value passed in (already promoted, or was never that value) → returns `False`, zero rows change (idempotency guard).
    - A disc has the `dvd1-*` fingerprint but NO `dvdread1-*` alias row → returns `False`, zero rows change.
    - Calling `promote_one_disc()` twice in a row for the same `dvd1-*` value → the second call returns `False` (idempotent re-run safety, proven by an actual double-invocation in the test, not just an assertion about the guard clause).
  </behavior>
  <action>
    Create `api/app/migrations_support.py` with a module docstring stating it holds Alembic-independent, directly pytest-testable per-row migration logic (per Pitfall 5 — CI never runs real Alembic against Postgres). Implement `promote_one_disc(connection: Connection, dvd1_fingerprint: str) -> bool` using `sqlalchemy.text()` raw SQL exactly per the RESEARCH sketch: `SELECT d.id AS disc_id, a.id AS alias_id, a.fingerprint AS dvdread1_fp FROM discs d JOIN disc_identity_aliases a ON a.disc_id = d.id WHERE d.fingerprint = :dvd1_fp AND a.fingerprint LIKE 'dvdread1-%'`; if no row, return `False`; else `DELETE FROM disc_identity_aliases WHERE id = :alias_id`, `UPDATE discs SET fingerprint = :new_fp WHERE id = :disc_id`, then `INSERT INTO disc_identity_aliases (id, disc_id, fingerprint, created_at) VALUES (...)` with a fresh `uuid.uuid4()` id and `datetime.now(timezone.utc)` for `created_at` (so the new alias row sorts after any pre-existing aliases, per D-06); return `True`. Write the 4 behaviors above as tests in `api/tests/test_promote_dvdread1_migration.py`, seeding real `Disc`/`DiscIdentityAlias` ORM rows via `db_session` then calling `promote_one_disc(db_session.connection(), <dvd1_value>)` directly — no Alembic invocation anywhere in this test file.
  </action>
  <acceptance_criteria>
    `grep -c "def promote_one_disc" api/app/migrations_support.py` returns 1.
    All 4 behaviors pass in `api/tests/test_promote_dvdread1_migration.py`.
  </acceptance_criteria>
  <verify>
    <automated>cd api && python -m pytest tests/test_promote_dvdread1_migration.py -v</automated>
  </verify>
  <done>promote_one_disc() exists, is idempotent, and is proven correct against the in-memory SQLite harness without invoking Alembic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: promote_all_dvdread1_discs() — bulk driver with per-disc commit</name>
  <files>api/app/migrations_support.py, api/tests/test_promote_dvdread1_migration.py</files>
  <read_first>
    api/app/migrations_support.py (Task 1's promote_one_disc, just written)
    05-RESEARCH.md's inlined Alembic `upgrade()` loop sketch (lines 493-511) — factor its loop BODY out into this testable function so Plan 05-06's eventual Alembic file becomes a thin wrapper only (Pitfall 5)
    05-RESEARCH.md Open Question #3 (lines 627-630) — periodic print-logging is sufficient at current scale; no separate checkpoint/resume bookkeeping needed beyond the idempotency guard already in promote_one_disc
  </read_first>
  <behavior>
    - Given 3 discs seeded with `dvd1-*` fingerprints, 2 of which also have a `dvdread1-*` alias → `promote_all_dvdread1_discs(connection)` returns `2` (the promoted count); the third disc's fingerprint is left untouched.
    - Re-running immediately after → returns `0` (fully idempotent bulk re-run, since both promoted discs now fail `promote_one_disc`'s idempotency guard).
    - Each promoted disc's write is durably committed independently (not merely staged in one uncommitted transaction): after the function returns, a FRESH query/session (not the same in-flight transaction) sees the promoted state — proves per-disc `connection.commit()` actually ran (SQLAlchemy 2.0 commit-as-you-go), not just a single commit at the very end.
  </behavior>
  <action>
    Add `promote_all_dvdread1_discs(connection: Connection) -> int` to `api/app/migrations_support.py`: enumerate candidates via `SELECT fingerprint FROM discs WHERE fingerprint LIKE 'dvd1-%'`, loop calling `promote_one_disc(connection, dvd1_fp)` for each, incrementing a counter on `True`, and calling `connection.commit()` after EVERY candidate (promoted or not, so the enumeration's own transaction segment never grows unbounded across a large table) — this is SQLAlchemy 2.0's documented "commit as you go" pattern: the connection auto-begins a new transaction segment on the next `execute()` after `commit()`. Add periodic `print(f"...promoted {promoted_count}/{i} discs processed")` progress logging every 100 rows processed (logging only — no separate checkpoint mechanism, per Open Question #3). Return the total promoted count. Add the 3 behaviors above to `api/tests/test_promote_dvdread1_migration.py`; for the "durably committed" behavior, open a SECOND `Session`/connection against the same test engine after `promote_all_dvdread1_discs` returns and assert the promoted state is visible there.
  </action>
  <acceptance_criteria>
    `grep -c "def promote_all_dvdread1_discs" api/app/migrations_support.py` returns 1.
    All 3 behaviors above pass.
  </acceptance_criteria>
  <verify>
    <automated>cd api && python -m pytest tests/test_promote_dvdread1_migration.py -v</automated>
  </verify>
  <done>promote_all_dvdread1_discs() exists, correctly promotes only eligible discs, commits per-disc, and is idempotent on re-run; Plan 05-06's Alembic migration can call it directly with no additional logic.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `api/app/migrations_support.py` (new module) — `promote_one_disc(connection, dvd1_fingerprint) -> bool`, `promote_all_dvdread1_discs(connection) -> int`
- `api/tests/test_promote_dvdread1_migration.py` (new file)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None — internal DB-state transform only | These functions take no external/network input; the only "input" is the DB's own existing fingerprint values, already validated at original submission time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | promote_one_disc / promote_all_dvdread1_discs | medium | mitigate | The idempotency guard (`WHERE discs.fingerprint = <current dvd1 value>`) prevents a double-run (accidental or malicious re-trigger) from corrupting state — a second invocation against an already-promoted disc is always a no-op (D-01). |
| T-05-05 | Denial of Service | promote_all_dvdread1_discs per-disc commit loop | low | accept | Row-by-row processing with per-row commits is slower than a bulk UPDATE but bounded, resumable, and never loses progress on interruption — acceptable given D-04's own "dataset still small pre-launch" framing, and the migration only ever runs inside the D-04 write-quiesce window (no concurrent live traffic contends with it). |
</threat_model>

<verification>
Run `cd api && python -m pytest tests/test_promote_dvdread1_migration.py -v` — all green, including the idempotent-double-run and fresh-session-visibility cases.
</verification>

<success_criteria>
promote_one_disc() and promote_all_dvdread1_discs() exist, are proven idempotent and resumable via direct SQLite-harness tests with zero Alembic invocation, and are ready to be wrapped by a thin Alembic migration file in Plan 05-06.
</success_criteria>

<output>
Create `.planning/phases/05-adr-0001-completion-dvdread1-promotion/05-02-SUMMARY.md` when done
</output>
