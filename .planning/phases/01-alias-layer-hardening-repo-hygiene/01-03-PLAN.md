---
phase: 01-alias-layer-hardening-repo-hygiene
plan: 03
type: tdd
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - api/app/routes/disc.py
  - api/tests/test_disc_submit.py
  - api/tests/test_dispute.py
autonomous: true
requirements: [VERIFY-02, IDENT-02]
must_haves:
  truths:
    - "A mismatched second submission against an already-verified disc leaves it verified (returns 200, records an audit DiscEdit) — never silently flips to disputed (VERIFY-02 crit #4)"
    - "A mismatched submission against an unverified disc still moves it to disputed via flag_dispute (the legitimate path)"
    - "verify_disc: unverified→verified transitions once; already-verified is idempotent 200 with NO extra DiscEdit; self-verify returns 403"
    - "Two submissions of the SAME new fingerprint converge to one disc row (disc-row race safe) — no split"
    - "flag_dispute in verification.py is the only writer of the disputed status across api/app/"
  artifacts:
    - api/app/routes/disc.py
    - api/tests/test_disc_submit.py
    - api/tests/test_dispute.py
  key_links:
    - "routes/disc.py imports verify/flag_dispute/resolve_dispute + VerificationTransitionError from app.verification (Plan 01)"
    - "VerificationTransitionError caught at each route boundary → _error_response with request_id (mirrors DiscIdentityConflict)"
    - "disc-row insert in submit_disc/register_disc wrapped in begin_nested + IntegrityError re-resolve (reuses Plan 02's pattern)"
---

<objective>
Wire the new guarded verification module (Plan 01) into `api/app/routes/disc.py` — replacing the five inline disc-status mutations (lines 241, 245, 422, 444, 616) with `verify()`, `flag_dispute()`, and `resolve_dispute()` calls — and extend IDENT-02 race-safety to the DISC-ROW insert at the two submission call sites (`submit_disc`, `register_disc`) using the same `begin_nested()` + `IntegrityError` re-resolve pattern hardened in Plan 02.

This is the phase's core BEHAVIOR CHANGE, not drift: line 444 today unconditionally forces the disc into the disputed state even when the disc is already verified — exactly the silent-flip VERIFY-02 success-criterion #4 forbids. Two existing passing tests ENCODE that bug (`test_disc_submit.py:115`, `:198`) plus `test_dispute.py:81`, because `seed_test_disc` seeds a verified disc (`conftest.py:154`). Per RESEARCH Pitfall 3, these MUST be updated as explicit tasks — this is a documented, intended behavior change. Per open-question A2 (recommended, confirmed here): a mismatched submission against a verified disc stays verified, records an audit `DiscEdit`, and returns 200 with a clear message — never 5xx, never disputed.

Per Pitfall 4, preserve the two distinct verified→verified contracts via `verify()`'s bool return: the `verify_disc` endpoint's idempotent 200 / NO DiscEdit, and the submit auto-verify path's "auto-verified" + edit. Do NOT introduce a 400 for verified→verify — the authoritative live behavior in `test_disc_verify.py` is idempotent 200, no edit (the D-12 research-note is resolved).

Purpose: Make `flag_dispute` the single grep-able writer of the disputed status and make the submission disc-row insert race-safe before Phase 5.
Output: rewired `routes/disc.py`; updated `test_disc_submit.py`/`test_dispute.py`; a new "verified-stays-verified" test; a disc-row race test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-alias-layer-hardening-repo-hygiene/01-CONTEXT.md
@.planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md
@.planning/phases/01-alias-layer-hardening-repo-hygiene/01-PATTERNS.md
@api/app/verification.py
@api/app/disc_identity.py
@api/app/routes/disc.py
@api/tests/conftest.py
@api/tests/test_disc_verify.py
</context>

## Artifacts this phase produces (new symbols — source-grounding reference)

- `api/app/verification.py` (Plan 01): `VerificationTransitionError`, `LEGAL_TRANSITIONS`, `verify`, `flag_dispute`, `resolve_dispute` — CONSUMED by this plan's routes.
- `FingerprintAliasResponse` + `fingerprint_aliases` (Plan 04); `_method_of` helper (Plan 04); `web/lib/api.ts` field (Plan 04).
- New tests this plan adds: a "verified-stays-verified on mismatch" case in `test_disc_submit.py`, and a disc-row losing-race convergence case.
- NOTE: `submit_disc`, `register_disc`, `verify_disc`, the `resolve_dispute` route, `_disc_to_response`, `_error_response`, `resolve_existing_disc_for_identities`, `DiscEdit`, `next_seq` are EXISTING symbols — this plan rewires them, it does not create them.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Rewrite the behavior-change tests to the guarded dispute contract</name>
  <files>api/tests/test_disc_submit.py, api/tests/test_dispute.py</files>
  <read_first>
    - api/tests/conftest.py (lines 130-190) — `seed_test_disc` seeds a verified disc (154); the reason lines 115/198/81 currently expect a disputed flip. You will need a helper or explicit keyword override to seed an unverified disc for the legitimate-dispute path.
    - api/tests/test_disc_submit.py (lines 100-210) — `test_submit_duplicate_fingerprint_conflicting_metadata` (115) and `test_duplicate_conflicting_metadata_disputes` (198): both drive a mismatched second submission against a verified seed and assert the disputed outcome (the bug).
    - api/tests/test_dispute.py (around line 81) — `test_submit_stores_conflict_data` uses `seeded_disc_with_owner` (verified) and asserts a disputed DiscEdit — same update needed.
    - api/tests/test_disc_verify.py (lines 60-90) — the idempotent-200 / no-extra-edit contract (`test_verify_already_verified_idempotent:71`, `test_verify_already_verified_no_extra_edit:82`) these MUST keep passing after wiring.
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md §"Pitfall 3" + §"Open Questions A2" — the intended contract.
  </read_first>
  <behavior>
    - Mismatched submission against an UNVERIFIED disc → status becomes disputed (legit path preserved) and a dispute DiscEdit is recorded.
    - Mismatched submission against an already-VERIFIED disc → status STAYS verified, response is 200, an audit DiscEdit is recorded, message is explicit (e.g. "cannot dispute a verified disc" / "conflict recorded"), and the response body never reports the disputed status (VERIFY-02 crit #4 / A2).
    - Two submissions of the same NEW fingerprint (disc-row losing race, deterministic injection) converge to exactly one disc row — no split.
  </behavior>
  <action>
    Update the three existing tests and add two new ones, per D-09 and A2. In `test_disc_submit.py:115` and `:198`: change the seed to an unverified disc (pass an explicit unverified-status keyword to `seed_test_disc`, or add a `seed_unverified_disc` helper) so they keep exercising the legitimate unverified→disputed path via `flag_dispute`. In `test_dispute.py:81`: apply the same unverified-seed update. ADD to `test_disc_submit.py` a new `test_mismatched_submission_against_verified_disc_stays_verified` asserting the disc remains verified, HTTP 200, an audit `DiscEdit` exists, and the response body does not report the disc as disputed (A2 contract). ADD a disc-row race test (deterministic injection per Plan 02's recipe — pre-insert the disc, or monkeypatch `resolve_existing_disc_for_identities` to return None on the first call and restore it in `finally`) asserting two same-fingerprint submissions converge to one `Disc` row. These MUST fail against current `routes/disc.py` (line 444 forces the disputed flip; the disc-row insert is not savepoint-guarded).
  </action>
  <verify>
    <automated>cd api && python -m pytest tests/test_disc_submit.py tests/test_dispute.py -x 2>&1 | tail -6 | grep -qiE "failed|error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `test_disc_submit.py` contains `test_mismatched_submission_against_verified_disc_stays_verified` asserting the disc stays verified, HTTP 200, and that the response body does not report the disputed status.
    - The two edited tests (115, 198) and `test_dispute.py:81` now seed unverified discs for the legitimate-dispute assertions.
    - A disc-row race test exists using deterministic injection; grep of the new/edited test region for `threading` and `asyncio.gather` returns nothing; any monkeypatch is restored in a `finally:` block.
    - `cd api && python -m pytest tests/test_disc_submit.py tests/test_dispute.py -x` FAILS (RED) against current routes.
  </acceptance_criteria>
  <done>Behavior-change tests rewritten to the guarded contract and RED against current code; new verified-stays-verified + disc-row-race tests present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Replace the 5 inline status mutations with verification.py calls + catch VerificationTransitionError</name>
  <files>api/app/routes/disc.py</files>
  <read_first>
    - api/app/routes/disc.py (lines 52-71 error envelope + `_identity_conflict_response`; 225-262 `resolve_dispute` incl. mutations 241/245; 405-459 `submit_disc` incl. mutations 422/444; 595-635 `verify_disc` incl. mutation 616) — the exact sites and the coarse role gate at line 232 that STAYS at the route (D-11).
    - api/app/verification.py — `verify`, `flag_dispute`, `resolve_dispute`, `VerificationTransitionError` (Plan 01) — the call targets and their bool contract.
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-PATTERNS.md §"routes/disc.py (MODIFY)" — the five-site replacement table and the route-boundary catch snippet.
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md §"Pattern 2", §"Pitfall 4", §"Code Examples" (route-boundary translation of `VerificationTransitionError`).
    - api/tests/test_disc_verify.py — the idempotent-200/no-edit + self-verify-403 contract to preserve.
  </read_first>
  <behavior>
    - resolve_dispute action="verify"/"reject" routes through `resolve_dispute(db, disc, actor, action)`; coarse role check stays at the route.
    - submit_disc metadata-match auto-verify calls `verify(db, existing, current_user)`; creates an auto-verify DiscEdit ONLY when it returns True.
    - submit_disc metadata-mismatch calls `flag_dispute(db, existing, current_user, reason)`; when it returns False (verified disc) the route keeps the disc verified, records an audit DiscEdit, returns 200 with an explicit message (A2); when True, the existing dispute path runs.
    - verify_disc calls `verify(db, disc, current_user)`; on `VerificationTransitionError` with attempted="verified" and a self-submitter → 403 "Cannot verify your own submission"; other transition errors → 409 via `_error_response`; when verify returns False → idempotent 200 "already verified" with NO DiscEdit; when True → create the "verify" DiscEdit + `next_seq` + commit.
  </behavior>
  <action>
    Replace all five inline status mutations (D-09 / VERIFY-02) with calls into `app.verification`, importing `verify`, `flag_dispute`, `resolve_dispute`, `VerificationTransitionError`. Map the route boundaries exactly as in PATTERNS §"routes/disc.py": wrap `verify()` calls in `try/except VerificationTransitionError` and translate to 403 (self-verify) or 409 via `_error_response(request_id, "invalid_state", str(exc), 409)`; branch on the bool return to preserve the idempotent-200/no-edit vs. auto-verify/edit DiscEdit behavior (Pitfall 4). For the submit-mismatch site (line 444), call `flag_dispute` and implement the A2 verified-disc contract (stay verified, audit DiscEdit, HTTP 200, explicit message) on the False return. Keep the coarse role gate (`role not in ("trusted","editor","admin")`) at the route (D-11). Do NOT add a 400 for verified→verify. The disputed-status write must, after this task, exist in exactly one place — inside `flag_dispute` in verification.py — with no direct status assignment remaining in `routes/disc.py`.
  </action>
  <verify>
    <automated>cd api && python -m pytest tests/test_disc_submit.py tests/test_dispute.py tests/test_disc_verify.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && python -m pytest tests/test_disc_submit.py tests/test_dispute.py tests/test_disc_verify.py -x` exits 0.
    - `grep -rnE 'status[[:space:]]*=[[:space:]]*"disputed"' api/app/` returns exactly one line, inside `api/app/verification.py` `flag_dispute`.
    - `grep -nE '\.status = "(verified|unverified|disputed)"' api/app/routes/disc.py` returns nothing (all five inline mutations removed).
    - `routes/disc.py` imports `verify, flag_dispute, resolve_dispute, VerificationTransitionError` from `app.verification`.
    - The verified-stays-verified test and `test_verify_already_verified_no_extra_edit` both pass.
  </acceptance_criteria>
  <done>All five status writes route through verification.py; flag_dispute is the sole disputed writer; verify/dispute contracts (incl. A2) hold; verify_disc idempotent/self-verify preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Wrap the disc-row insert in submit_disc/register_disc with savepoint + IntegrityError re-resolve (IDENT-02)</name>
  <files>api/app/routes/disc.py, api/tests/test_disc_submit.py</files>
  <read_first>
    - api/app/routes/disc.py (submit_disc new-disc branch ~466-501; register_disc ~347-362) — the two `Disc(...)` + `db.add()` sites to wrap.
    - api/app/disc_identity.py — `resolve_existing_disc_for_identities` (the re-resolve target) and Plan 02's now-hardened savepoint pattern to mirror.
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md §"Code Examples" (Wrapping the disc-row insert in submit_disc) + §"Pitfall 2".
    - api/tests/test_disc_submit.py — the disc-row race test from Task 1 (the GREEN target here).
  </read_first>
  <behavior>
    - Two submissions racing the SAME new primary fingerprint: the loser catches IntegrityError, re-resolves via `resolve_existing_disc_for_identities`, and falls through to the existing "duplicate submission" branch (auto-verify / conflict) instead of creating a split disc row.
  </behavior>
  <action>
    Wrap the new-`Disc` insert in `submit_disc` and `register_disc` in `with db.begin_nested():` + `db.add(disc)` + `db.flush()` inside a `try` guarded by `sqlalchemy.exc.IntegrityError` (import `IntegrityError` from `sqlalchemy.exc`). On conflict: call `db.expire_all()` (Pitfall 2), then `winner = resolve_existing_disc_for_identities(db, body.fingerprint, body.fingerprint_aliases)`, then continue through the existing duplicate-submission handling (auto-verify via `verify()` / conflict via `DiscIdentityConflict`). Catch `IntegrityError` specifically — do not catch a broad base exception. This makes the disc-row race test from Task 1 converge to a single disc row.
  </action>
  <verify>
    <automated>cd api && python -m pytest tests/test_disc_submit.py -x && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && python -m pytest tests/test_disc_submit.py -x` exits 0, including the disc-row losing-race convergence test.
    - `grep -nE 'begin_nested|IntegrityError' api/app/routes/disc.py` shows the disc-row insert path guarded at the submission sites.
    - Full API suite green: `cd api && python -m pytest tests/ -q` exits 0 (VERIFY-02 ripple into submit/dispute/verify fully resolved).
    - `grep -n 'except Exception' api/app/routes/disc.py` returns nothing new around the disc-row insert.
  </acceptance_criteria>
  <done>Disc-row inserts are savepoint-guarded and converge under the deterministic race test; the entire API suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → submit/verify endpoints | Untrusted submitter crafts a mismatched-metadata submission or a self-verify; the route must delegate every status decision to verification.py |
| concurrent workers → disc-row fingerprint | Two workers may race a new primary-fingerprint disc insert; DB UNIQUE arbitrates, the route converges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-V1 | Tampering / Repudiation | `submit_disc` mismatch branch (line 444 today) | high | mitigate | Replace the unconditional disputed flip with `flag_dispute`, which refuses a verified disc (A2 contract: stay verified, audit DiscEdit, 200). Verified by the new verified-stays-verified test. Closes VERIFY-02 crit #4. |
| T-01-R1 | Tampering / DoS-of-data-quality | disc-row insert in `submit_disc`/`register_disc` | high | mitigate | `begin_nested()` savepoint + catch `IntegrityError` + re-resolve → single disc row under the deterministic race test (IDENT-02). |
| T-01-V2 | Elevation of Privilege | `verify_disc` self-verification | medium | mitigate | Self-verify invariant enforced inside `verify()` (Plan 01, D-11); route translates to 403. Coarse role gate stays at the route. |
| T-01-SC | Tampering (supply chain) | pip installs | low | accept | No packages installed this phase (Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `cd api && python -m pytest tests/ -q` — full API suite green (per 01-VALIDATION.md per-wave merge gate).
- `grep -rnE 'status[[:space:]]*=[[:space:]]*"disputed"' api/app/` — exactly one hit (flag_dispute in verification.py).
- `grep -nE '\.status = "' api/app/routes/disc.py` — no inline status mutations remain.
- Manual/load (deferred, non-blocking): true multi-worker Postgres concurrency per 01-VALIDATION.md Manual-Only table.
</verification>

<success_criteria>
- All five inline status mutations are replaced by verification.py calls; `flag_dispute` is the sole disputed writer across `api/app/` (VERIFY-02).
- A verified disc stays verified on a mismatched submission (200 + audit DiscEdit, never disputed) — VERIFY-02 crit #4.
- verify_disc idempotent-200/no-edit and self-verify-403 contracts preserved (no 400 for verified→verify).
- Disc-row inserts converge to a single row under the deterministic losing-race test (IDENT-02).
- Full API suite green.
</success_criteria>

<output>
Create `.planning/phases/01-alias-layer-hardening-repo-hygiene/01-03-SUMMARY.md` when done.
</output>
