---
phase: 01-alias-layer-hardening-repo-hygiene
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - fix_test.py
  - fix_test2.py
  - test_script.py
  - verify_t11.py
  - run_uat.py
  - create_uat_dirs.py
  - scripts/run_uat.py
  - scripts/create_uat_dirs.py
  - uat_results.json
  - uat_dirs/
autonomous: true
requirements: [CLEAN-01, CLEAN-02]
must_haves:
  truths:
    - "Repo root contains none of fix_test.py, fix_test2.py, test_script.py, verify_t11.py (CLEAN-01, success-criterion #5)"
    - "run_uat.py and create_uat_dirs.py are relocated under scripts/ and still importable/functional (D-18)"
    - "uat_results.json and uat_dirs/ are git-untracked (git rm --cached) AND gitignored — git ls-files shows none (CLEAN-02)"
    - "The full test suite still passes after the removals (nothing load-bearing was deleted)"
  artifacts:
    - .gitignore
    - scripts/run_uat.py
    - scripts/create_uat_dirs.py
  key_links:
    - "gitignore alone does NOT untrack already-tracked files — git rm --cached is required in the same change (RESEARCH Pitfall 5)"
    - "Import-safety confirmed at plan time: no *.py imports fix_test/fix_test2/test_script/verify_t11 (grep returned empty)"
---

<objective>
Repo hygiene (CLEAN-01, CLEAN-02) — pure VCS/version-control work, no runtime code. Per D-17 delete the four disposable one-shot patch scripts at repo root (`fix_test.py`, `fix_test2.py`, `test_script.py`, `verify_t11.py`) whose edits are already applied (CONCERNS.md) and which nothing imports (planner confirmed import-safety returned empty this session). Per D-18 relocate the still-useful UAT tooling (`run_uat.py`, `create_uat_dirs.py`) under `scripts/` rather than deleting it, so nothing load-bearing is lost. Per D-19 gitignore the generated UAT artifacts (`uat_results.json`, `uat_dirs/`).

Per RESEARCH Pitfall 5 (the load-bearing detail): `uat_results.json` and the entire `uat_dirs/` tree (20 tracked BDMV/VIDEO_TS fixture files) are CURRENTLY GIT-TRACKED — a `.gitignore` entry alone will NOT untrack them. The change MUST pair `.gitignore` additions with `git rm --cached uat_results.json` and `git rm -r --cached uat_dirs/` so they become untracked (success-criterion #5 requires them gitignored/untracked, not merely ignored-by-name). Confirm `git ls-files` reports none of the `uat_` paths afterward.

Purpose: Remove ad-hoc repo cruft so the codebase is clean before BD fingerprinting / promotion / OAuth build on top of it — the hygiene half of CONCERNS.md.
Output: 4 scripts deleted, 2 scripts relocated under `scripts/`, `.gitignore` updated, UAT artifacts untracked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-alias-layer-hardening-repo-hygiene/01-CONTEXT.md
@.planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
</context>

## Artifacts this phase produces (new symbols — source-grounding reference)

- NEW file locations in this plan: `scripts/run_uat.py`, `scripts/create_uat_dirs.py` (relocated, not new code); `.gitignore` gains `uat_results.json` and `uat_dirs/` entries.
- REMOVED: `fix_test.py`, `fix_test2.py`, `test_script.py`, `verify_t11.py` (deleted); `uat_results.json` + `uat_dirs/` (untracked, kept on disk but gitignored).
- From other Phase-1 plans (unrelated to this plan's files): `api/app/verification.py` (Plan 01), race-safe `disc_identity.py` (Plan 02), `FingerprintAliasResponse`/`fingerprint_aliases` (Plan 04), the new test files (Plans 03/04/05). This plan touches NO `api/` or `web/` source, so it has zero file overlap with them (Wave 1 parallel-safe).

<tasks>

<task type="auto">
  <name>Task 1 (CLEAN-01): Delete the one-shot patch scripts and relocate UAT tooling under scripts/</name>
  <files>fix_test.py, fix_test2.py, test_script.py, verify_t11.py, scripts/run_uat.py, scripts/create_uat_dirs.py</files>
  <read_first>
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md §"Runtime State Inventory" — the confirmed tracked scripts, sizes, and the import-safety requirement.
    - run_uat.py and create_uat_dirs.py — read them before relocating, to confirm they are still-useful UAT tooling (D-18) and to spot any hardcoded relative path to `uat_dirs/`/`uat_results.json` that must be adjusted after the move.
    - fix_test.py, fix_test2.py, test_script.py, verify_t11.py — skim to confirm each is a disposable one-shot whose effect is already applied (D-17) before deleting.
  </read_first>
  <action>
    First re-confirm import-safety (no residual imports of the four one-shot scripts anywhere under the tree) — the no-wave-off rule requires the executor to re-verify, not trust the plan. Then `git rm` the four one-shot scripts: `fix_test.py`, `fix_test2.py`, `test_script.py`, `verify_t11.py` (D-17). Create the `scripts/` directory if absent and `git mv run_uat.py scripts/run_uat.py` and `git mv create_uat_dirs.py scripts/create_uat_dirs.py` (D-18). If either relocated script hardcodes a repo-root-relative path to `uat_dirs/` or `uat_results.json`, update it so the script still resolves those paths from its new location (resolve against the repo root); otherwise leave the logic unchanged. Confirm each relocated script still parses (byte-compile) and imports cleanly from its new path — do not leave a broken dev tool.
  </action>
  <verify>
    <automated>test ! -e fix_test.py && test ! -e fix_test2.py && test ! -e test_script.py && test ! -e verify_t11.py && test -f scripts/run_uat.py && test -f scripts/create_uat_dirs.py && python -m py_compile scripts/run_uat.py scripts/create_uat_dirs.py && echo CLEAN01-OK</automated>
  </verify>
  <acceptance_criteria>
    - `fix_test.py`, `fix_test2.py`, `test_script.py`, `verify_t11.py` no longer exist at repo root (the four `test ! -e` checks pass).
    - `scripts/run_uat.py` and `scripts/create_uat_dirs.py` exist and byte-compile (`python -m py_compile` exits 0).
    - `git status --porcelain` shows the four deletions (`D`) and the two renames (`R`) staged/tracked — the moves are recorded by git, not left as untracked copies.
    - Import-safety re-confirmed: a grep of `*.py` for imports of the four deleted script names returns empty.
  </acceptance_criteria>
  <done>The four one-shot scripts are removed and the two UAT tools are relocated under scripts/ and still function.</done>
</task>

<task type="auto">
  <name>Task 2 (CLEAN-02): Untrack the UAT artifacts and add .gitignore entries</name>
  <files>.gitignore, uat_results.json, uat_dirs/</files>
  <read_first>
    - .gitignore — the current ignore rules, so the new entries are appended in the file's existing style without duplicating an existing rule.
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md §"Runtime State Inventory" + §"Pitfall 5" — the exact `.gitignore` additions and the mandatory `git rm --cached` step (gitignore alone will not untrack tracked files).
  </read_first>
  <action>
    Add `uat_results.json` and `uat_dirs/` to `.gitignore` (D-19). In the SAME change, untrack the already-tracked artifacts with `git rm --cached uat_results.json` and `git rm -r --cached uat_dirs/` (Pitfall 5) so they leave the index while remaining on disk. Do NOT delete them from disk (they are regeneratable dev fixtures, and the relocated `create_uat_dirs.py` produces them). After this, the `uat_` paths must no longer appear in `git ls-files`.
  </action>
  <verify>
    <automated>git ls-files | grep -qE '^(uat_results\.json|uat_dirs/)' && echo STILL-TRACKED-FAIL || (grep -q 'uat_results.json' .gitignore && grep -q 'uat_dirs/' .gitignore && echo CLEAN02-OK)</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files` reports no `uat_results.json` and no `uat_dirs/...` paths (all 21 previously-tracked UAT paths untracked).
    - `.gitignore` contains both `uat_results.json` and `uat_dirs/` entries.
    - `uat_results.json` and `uat_dirs/` still exist on disk (untracked, not deleted) — `git status` shows them as ignored, not as deletions of working-tree content.
    - `git check-ignore uat_results.json uat_dirs/` returns both paths (they are now ignored).
  </acceptance_criteria>
  <done>UAT artifacts are untracked via git rm --cached and gitignored; git ls-files shows none of them; files remain on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm nothing load-bearing was removed — suite still green</name>
  <files>api/tests/ (test-suite run — no file changes unless a broken reference is found and fixed inline)</files>
  <read_first>
    - .planning/phases/01-alias-layer-hardening-repo-hygiene/01-RESEARCH.md §"Runtime State Inventory" — the confirmation that no scheduler/import/build artifact depends on the removed scripts.
  </read_first>
  <action>
    Run the API test suite to confirm the deletions/relocations broke nothing that was load-bearing (the removed scripts are one-shot cruft, but the no-wave-off rule requires proving it). If any test or tool referenced a removed/relocated path, fix the reference inline (do not defer). This task changes no product code unless a genuine broken reference is found.
  </action>
  <verify>
    <automated>cd api && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && python -m pytest tests/ -q` exits 0 (no test depended on the removed root scripts or the moved UAT tooling).
    - No new broken import/reference remains anywhere pointing at a deleted or relocated path.
  </acceptance_criteria>
  <done>The full API suite is green after the hygiene changes; nothing load-bearing was removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index | Tracked ad-hoc scripts and generated UAT artifacts cross into version control; hygiene keeps disposable/regeneratable content out of the tracked history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-C1 | Information Disclosure / repo integrity | tracked root debug scripts + generated UAT artifacts | low | mitigate | Delete the one-shot scripts (D-17), relocate real tooling under `scripts/` (D-18), and `git rm --cached` + gitignore the generated artifacts (D-19) so disposable/regeneratable content is not carried in tracked history. Import-safety re-confirmed before deletion (no-wave-off). |
| T-01-SC | Tampering (supply chain) | pip/npm installs | low | accept | No packages installed this phase (Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `git ls-files` — no `uat_results.json`/`uat_dirs/` paths; no `fix_test.py`/`fix_test2.py`/`test_script.py`/`verify_t11.py`.
- `git check-ignore uat_results.json uat_dirs/` — both returned (ignored).
- `python -m py_compile scripts/run_uat.py scripts/create_uat_dirs.py` — relocated tools parse.
- `cd api && python -m pytest tests/ -q` — full suite green (nothing load-bearing removed).
</verification>

<success_criteria>
- Repo root contains none of the four named debug scripts (CLEAN-01, success-criterion #5).
- `run_uat.py`/`create_uat_dirs.py` relocated under `scripts/` and still functional (D-18).
- `uat_results.json` and `uat_dirs/` are untracked (git rm --cached) and gitignored — `git ls-files` shows none (CLEAN-02).
- Full API suite green after the changes.
</success_criteria>

<output>
Create `.planning/phases/01-alias-layer-hardening-repo-hygiene/01-06-SUMMARY.md` when done.
</output>
