---
phase: 06-oauth-account-linking
plan: 07
type: execute
wave: 5
depends_on: ["06-05", "06-06", "06-03", "06-02"]
files_modified:
  - docs/auth-setup.md
  - mkdocs.yml
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "docs/auth-setup.md is published and reachable from the mkdocs nav (DOCS-03)."
    - "The guide documents per-provider registration for the four headline providers, required env vars, the OVID_ENV breaking-change/boot requirement, the Mastodon requests-vs-httpx note, the Apple .p8 rotation runbook, and the IndieAuth opt-in."
  artifacts:
    - "docs/auth-setup.md (new) with all six DOCS-03 sections + the Mastodon DNS-rebinding residual-risk note"
    - "mkdocs.yml nav entry pointing at auth-setup.md"
  key_links:
    - "the guide reflects the ACTUAL implemented behavior (env var names, OVID_ENV values, OVID_ENABLE_INDIEAUTH) from Plans 03/05/06 — written last so it does not drift from what shipped"
---

<objective>
Publish `docs/auth-setup.md` (DOCS-03): the OAuth setup guide covering per-provider registration, the
required env vars, and the operational notes this phase introduced. It is written last (Wave 5) so it
documents exactly what shipped — the OVID_ENV breaking change, the Apple short-lived-secret rotation
model, the IndieAuth opt-in flag, and the Mastodon httpx divergence + SSRF residual note.

Purpose: Give operators a single authoritative page to configure and upgrade OAuth, and record the
deliberate deviations (Mastodon httpx, DNS-rebinding residual) so future contributors don't undo them.
Output: docs/auth-setup.md + mkdocs.yml nav entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oauth-account-linking/06-CONTEXT.md
@.planning/phases/06-oauth-account-linking/06-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- `docs/auth-setup.md` (new) — the OAuth setup + operations guide
- `mkdocs.yml` nav entry for the new page

<tasks>

<task type="auto">
  <name>Task 1: Write docs/auth-setup.md</name>
  <files>docs/auth-setup.md</files>
  <read_first>
    - .planning/phases/06-oauth-account-linking/06-RESEARCH.md (§ "DOCS-03: docs/auth-setup.md content requirements" — the six required content blocks; § Don't Hand-Roll Mastodon row for the httpx note wording)
    - .env.example (the exact env var names: GITHUB_CLIENT_ID/SECRET, GOOGLE_CLIENT_ID/SECRET, APPLE_CLIENT_ID/TEAM_ID/KEY_ID/PRIVATE_KEY, OVID_ENV, OVID_ENABLE_INDIEAUTH — cross-reference, do not invent)
    - docs/self-hosting.md and docs/deployment.md (existing doc tone/structure to match; where the Apple .p8 rotation + OVID_ENV upgrade note should cross-link)
    - .planning/phases/06-oauth-account-linking/06-CONTEXT.md (D-08/D-09/D-10 — the exact operator-facing semantics to document)
  </read_first>
  <action>
    Create `docs/auth-setup.md` with these sections (per RESEARCH.md DOCS-03 requirements):
    1. Per-provider registration steps for the four headline providers: GitHub OAuth App, Google Cloud OAuth
       Client, Apple Sign-In Service ID + Key (.p8 download), and Mastodon (document that registration is
       AUTOMATIC per-instance via dynamic POST /api/v1/apps — no manual app creation, unlike the other three).
    2. Required env vars for each provider, using the exact names from .env.example (cross-reference it).
    3. The new OVID_ENV requirement (D-09): flag prominently as a BREAKING change for existing self-hosted
       instances — accepted values (development|production), that the API refuses to boot if it is unset or
       unrecognized, and that production disables the IndieAuth localhost bypass.
    4. The Mastodon requests-vs-httpx note (verbatim intent from DOCS-03): OVID's Mastodon OAuth is hand-rolled
       with httpx (for HTTP-client consistency), deliberately NOT the Mastodon.py/requests package — a
       documented deviation from the original STACK.md recommendation so a future contributor doesn't revert
       it. Include the AUTH-05 SSRF posture and the documented DNS-rebinding residual-risk note from Plan 02
       (dual-stack validation is enforced; full IP-pinning is deferred for v0.2.0 as an accepted residual).
    5. The Apple .p8 key rotation runbook (D-10): register a new key in the Apple Developer console, update
       APPLE_KEY_ID/APPLE_PRIVATE_KEY, restart the API (no code change — generate_apple_client_secret rebuilds
       the short-lived client secret from current env values every token exchange).
    6. The IndieAuth opt-in note (D-08): disabled by default, enabled via OVID_ENABLE_INDIEAUTH (truthy), and
       that it is not one of the four headline providers surfaced elsewhere.
    Match the existing docs/ style (headings, admonitions available via the pymdownx.superfences/admonition
    extensions already configured in mkdocs.yml).
  </action>
  <acceptance_criteria>
    - docs/auth-setup.md exists and contains sections for GitHub, Google, Apple, Mastodon registration; required env vars; the OVID_ENV breaking-change requirement; the Mastodon httpx note; the Apple .p8 rotation runbook; and the IndieAuth opt-in.
    - Env var names in the doc match .env.example exactly (GITHUB_CLIENT_ID, GOOGLE_CLIENT_ID, APPLE_KEY_ID, OVID_ENV, OVID_ENABLE_INDIEAUTH, etc.).
    - The OVID_ENV section explicitly labels it a breaking change and states the refuse-to-boot behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/trekkie/projects/OVID && test -f docs/auth-setup.md && grep -qi 'OVID_ENV' docs/auth-setup.md && grep -qi 'breaking' docs/auth-setup.md && grep -qi 'httpx' docs/auth-setup.md && grep -qi 'OVID_ENABLE_INDIEAUTH' docs/auth-setup.md && grep -qi 'p8\|\.p8\|rotation' docs/auth-setup.md && grep -qiE 'github|google|apple|mastodon' docs/auth-setup.md && echo DOCS_OK</automated>
  </verify>
  <done>docs/auth-setup.md is published with all six DOCS-03 sections plus the Mastodon SSRF residual note, using exact env var names.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth-setup.md to mkdocs nav</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (nav: block, lines 31-46 — the Technical section at line 39-41 with api-reference.md / fingerprint-spec.md is the natural home)
  </read_first>
  <action>
    Add an entry for the new page to the `nav:` block in mkdocs.yml — place it under the existing `Technical:`
    section (alongside `API Reference` and `Fingerprint Spec`) as e.g. `- Auth Setup: auth-setup.md`, matching
    the existing two-space-indent list style. Do not reorder or remove existing nav entries.
  </action>
  <acceptance_criteria>
    - mkdocs.yml nav references auth-setup.md.
    - No existing nav entry is removed or reordered.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/trekkie/projects/OVID && grep -q 'auth-setup.md' mkdocs.yml && grep -q 'api-reference.md' mkdocs.yml && grep -q 'fingerprint-spec.md' mkdocs.yml && echo NAV_OK</automated>
  </verify>
  <done>auth-setup.md is reachable from the mkdocs nav under Technical; existing entries preserved.</done>
</task>

</tasks>

<verification>
- Task 1 + Task 2 automated greps pass (all six DOCS-03 sections present; nav wired).
- Optional manual: `mkdocs build --strict` succeeds if mkdocs is installed locally (no broken links).
</verification>

<success_criteria>
- docs/auth-setup.md is published, in the mkdocs nav, and covers all six DOCS-03 content blocks.
- The OVID_ENV breaking change, Apple .p8 rotation runbook, Mastodon httpx note + SSRF residual, and IndieAuth opt-in are all documented and match what shipped.
</success_criteria>

<output>
Create `.planning/phases/06-oauth-account-linking/06-07-SUMMARY.md` when done.
</output>
