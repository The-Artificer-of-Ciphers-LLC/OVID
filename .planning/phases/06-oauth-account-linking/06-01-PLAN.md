---
phase: 06-oauth-account-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/models.py
  - api/alembic/versions/900000000007_pending_account_links.py
autonomous: true
requirements: [AUTH-08]
must_haves:
  truths:
    - "A PendingAccountLink row can be created carrying existing_user_id, new_provider, new_provider_id, expires_at, consumed_at (D-01)."
    - "The row is the server-side state carrier for a confirm-gated merge — not session state — giving TTL + single-use + audit semantics at the DB layer."
  artifacts:
    - "api/app/models.py contains a PendingAccountLink ORM class mapped to table pending_account_links"
    - "api/alembic/versions/900000000007_pending_account_links.py creates the table with down_revision 900000000006"
  key_links:
    - "PendingAccountLink.existing_user_id is a ForeignKey to users.id (ondelete CASCADE) so orphan pending links cannot outlive their account"
    - "Base.metadata registers pending_account_links so the existing conftest _reset_tables (create_all) fixture picks it up automatically"
---

<objective>
Add the `PendingAccountLink` ORM model and its Alembic migration `900000000007`. This is the
server-side state carrier that replaces the nOAuth-vulnerable session-carried pending-link
mechanism (D-01). It is the Wave 1 foundation the merge-confirmation logic (Plan 04) and the
route wiring (Plan 05) build on.

Purpose: Give the confirm-gated merge a durable, auditable, single-use, TTL-bearing row keyed by
`existing_user_id` instead of "whatever account logs in next in this session."
Output: `PendingAccountLink` class in `api/app/models.py`; migration `900000000007`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-oauth-account-linking/06-CONTEXT.md
@.planning/phases/06-oauth-account-linking/06-RESEARCH.md
@.planning/phases/06-oauth-account-linking/06-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- `PendingAccountLink` ORM class — `api/app/models.py`
- Alembic migration `900000000007_pending_account_links.py` (table `pending_account_links`, index `idx_pending_account_links_existing_user`)

<tasks>

<task type="auto">
  <name>Task 1: Add PendingAccountLink ORM model</name>
  <files>api/app/models.py</files>
  <read_first>
    - api/app/models.py (UserOAuthLink at lines 370-391 — the exact analog: UUID PK, _utcnow default, ForeignKey(..., ondelete="CASCADE"), __table_args__ shape; also the top-of-file imports to reuse)
    - .planning/phases/06-oauth-account-linking/06-PATTERNS.md (§ "api/app/models.py — add PendingAccountLink" — the field list and what to change vs UserOAuthLink)
    - .planning/phases/06-oauth-account-linking/06-RESEARCH.md (§ Pattern 3 — column semantics and single-use-via-consumed_at rationale)
  </read_first>
  <action>
    Add a `PendingAccountLink(Base)` class alongside `UserOAuthLink` in `api/app/models.py`, per D-01.
    Table name `pending_account_links`. Columns, reusing the imports already present at the top of
    models.py (uuid, UUID postgres type, Mapped, mapped_column, ForeignKey, DateTime, String, Index,
    _utcnow) — do not re-import:
    - `id`: `Mapped[uuid.UUID]` UUID primary key, `default=uuid.uuid4` (copy UserOAuthLink's id column).
    - `existing_user_id`: `Mapped[uuid.UUID]`, `ForeignKey("users.id", ondelete="CASCADE")`, nullable=False.
    - `new_provider`: `Mapped[str]` `String(30)`, nullable=False.
    - `new_provider_id`: `Mapped[str]` `String(255)`, nullable=False.
    - `created_at`: `Mapped[datetime]` `DateTime(timezone=True)`, `default=_utcnow`.
    - `expires_at`: `Mapped[datetime]` `DateTime(timezone=True)`, nullable=False (TTL; no default — callers set it).
    - `consumed_at`: `Mapped[datetime | None]` `DateTime(timezone=True)`, nullable (single-use marker; set on consume).
    Add `__table_args__ = (Index("idx_pending_account_links_existing_user", "existing_user_id"),)`.
    Do NOT add a UniqueConstraint — multiple pending offers per user over time are valid; single-use is
    enforced by `consumed_at`, not DB uniqueness (per PATTERNS.md). No relationship back_populates is
    required (Plan 04 loads rows by id/existing_user_id via explicit queries).
  </action>
  <acceptance_criteria>
    - `from app.models import PendingAccountLink` succeeds.
    - `PendingAccountLink.__tablename__ == "pending_account_links"`.
    - The mapped table exposes exactly the columns: id, existing_user_id, new_provider, new_provider_id, created_at, expires_at, consumed_at.
    - `existing_user_id` column has a ForeignKey to `users.id` with ondelete CASCADE.
    - `expires_at` is NOT nullable; `consumed_at` IS nullable.
    - No `UniqueConstraint` is declared on the model.
  </acceptance_criteria>
  <verify>
    <automated>cd api && .venv/bin/python -c "import uuid, datetime as dt; from sqlalchemy import create_engine; from sqlalchemy.orm import Session; from app.database import Base; from app.models import User, PendingAccountLink; e=create_engine('sqlite://'); Base.metadata.create_all(e); s=Session(e); u=User(username='u', email='u@x.com', display_name='U', role='contributor'); s.add(u); s.flush(); p=PendingAccountLink(existing_user_id=u.id, new_provider='github', new_provider_id='gh_1', expires_at=dt.datetime.now(dt.timezone.utc)+dt.timedelta(minutes=10)); s.add(p); s.commit(); r=s.query(PendingAccountLink).one(); assert r.consumed_at is None; assert str(r.existing_user_id)==str(u.id); assert 'pending_account_links' in Base.metadata.tables; print('OK')"</automated>
  </verify>
  <done>The PendingAccountLink model imports, maps to pending_account_links, round-trips a row on in-memory SQLite with consumed_at defaulting to NULL, and is registered in Base.metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add Alembic migration 900000000007</name>
  <files>api/alembic/versions/900000000007_pending_account_links.py</files>
  <read_first>
    - api/alembic/versions/800000000000_mastodon_oauth_clients.py (exact analog — simple create_table, no backfill, revision/down_revision header shape)
    - api/alembic/versions/900000000006_promote_dvdread1_primary.py (confirm this is the current head; its revision id is the down_revision for the new migration)
    - .planning/phases/06-oauth-account-linking/06-PATTERNS.md (§ migration — the full copy-ready upgrade/downgrade body)
  </read_first>
  <action>
    Create `api/alembic/versions/900000000007_pending_account_links.py` following the
    `800000000000_mastodon_oauth_clients.py` style exactly. Set `revision = '900000000007'` and
    `down_revision = '900000000006'` (confirmed current head). `upgrade()` calls `op.create_table(
    'pending_account_links', ...)` with columns matching the ORM model: id sa.UUID() PK not-null,
    existing_user_id sa.UUID() not-null with `sa.ForeignKeyConstraint(['existing_user_id'], ['users.id'],
    ondelete='CASCADE')`, new_provider sa.String(length=30) not-null, new_provider_id sa.String(length=255)
    not-null, created_at sa.DateTime(timezone=True) not-null, expires_at sa.DateTime(timezone=True)
    not-null, consumed_at sa.DateTime(timezone=True) nullable, plus `sa.PrimaryKeyConstraint('id')`. Then
    `op.create_index('idx_pending_account_links_existing_user', 'pending_account_links',
    ['existing_user_id'])`. `downgrade()` drops the index then the table. No raw text() SQL and no
    backfill — this is a brand-new empty table, so no SQLite/Postgres shim is needed (per PATTERNS.md).
  </action>
  <acceptance_criteria>
    - The migration file exists at the exact path with revision '900000000007' and down_revision '900000000006'.
    - `alembic history` shows 900000000007 as the new head with 900000000006 as its parent (single linear chain, no branch).
    - The migration column set and the ORM model column set are identical (names, types, nullability, FK, index).
    - downgrade() drops both the index and the table.
  </acceptance_criteria>
  <verify>
    <automated>cd api && grep -q "revision: str = '900000000007'" alembic/versions/900000000007_pending_account_links.py && grep -q "down_revision: Union\[str, None\] = '900000000006'" alembic/versions/900000000007_pending_account_links.py && DATABASE_URL="sqlite://" OVID_SECRET_KEY="x" .venv/bin/python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; c=Config('alembic.ini'); s=ScriptDirectory.from_config(c); heads=s.get_heads(); assert heads==('900000000007',), heads; rev=s.get_revision('900000000007'); assert rev.down_revision=='900000000006'; print('OK head', heads)"</automated>
  </verify>
  <done>Migration 900000000007 exists, is the single Alembic head, chains to 900000000006, and its DDL matches the ORM model exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth callback → merge-state storage | The merge decision must be persisted where a client/session cannot forge or redirect it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Spoofing / Elevation | merge-state carrier | high | mitigate | State lives in a DB row keyed by `existing_user_id` (this plan) — not session — removing the "next login in this session merges anywhere" nOAuth vector. The ownership check that consumes it is Plan 04. |
| T-06-02 | Information disclosure | orphaned pending links | low | mitigate | `existing_user_id` FK uses `ondelete=CASCADE`, so pending offers cannot outlive a deleted account. |
</threat_model>

<verification>
- `cd api && .venv/bin/python -m pytest tests/ -q` — full existing API suite still green (new model/migration must not regress any existing behavior).
- Model import + row round-trip (Task 1 verify) and Alembic head/chain check (Task 2 verify) both pass.
</verification>

<success_criteria>
- `PendingAccountLink` model and migration `900000000007` exist and agree column-for-column.
- Alembic head is `900000000007` chaining to `900000000006`.
- Full existing API test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/06-oauth-account-linking/06-01-SUMMARY.md` when done.
</output>
