---
phase: 07-web-ui-production-readiness
plan: 08
type: execute
wave: 3
depends_on: ["07-04", "07-05", "07-06", "07-07"]
files_modified:
  - .env.example
  - docs/deployment.md
autonomous: false
requirements: [WEBUI-01]
user_setup:
  - service: redshirt reverse proxy (host infra)
    why: "TLS termination + routing for the staging web origin (external infra, not automatable)"
    dashboard_config:
      - task: "Route the staging web hostname to the Next standalone container (:3100) with TLS"
        location: "redshirt proxy config on the deployment host"
      - task: "Add DNS for the staging web + staging API hostnames"
        location: "DNS provider for oviddb.org"
must_haves:
  truths:
    - "The staging web origin is documented as an allowlisted CORS_ORIGINS entry so login + the D-04 merge redirect do not fail closed (Pitfall 2)"
    - "The staging NEXT_PUBLIC_API_URL is documented as a distinct build arg baked into the staging web image (Pitfall 3)"
    - "The full web + api test suites are green as the phase gate before staging sign-off"
    - "A human verifies the staging deploy end-to-end over TLS: search, disc detail + aliases, submit, settings add/remove + merge redirect, and the D-03 a11y floor (keyboard + AA contrast in both light and dark) — D-06 'verifiably deployable + live on staging' (D-03 blocking gate)"
  artifacts:
    - docs/deployment.md
  key_links:
    - "staging web origin ↔ CORS_ORIGINS (_validate_web_redirect_uri fails closed if missing)"
    - "staging NEXT_PUBLIC_API_URL ↔ web/Dockerfile build ARG (baked at build, not runtime)"
---

<objective>
Make Phase 7 "verifiably deployable + live on staging" (D-06). Wire and document the staging deployment
env — the staging web origin in CORS_ORIGINS (or login and the D-04 merge redirect fail closed, Pitfall 2)
and a distinct staging NEXT_PUBLIC_API_URL build arg (baked at build, Pitfall 3) — run the full test
suites as the phase gate, then human-verify the live staging deploy end-to-end over TLS including the
D-03 accessibility floor.

Purpose: D-06 scopes Phase 7 to staging (the public oviddb.org apex cutover + domain redirects + DB
seeding are Phase 8). This plan closes the deploy + a11y-sign-off half of WEBUI-01's success criterion.
Output: staging env docs + .env.example entries + a green phase gate + a human staging/a11y sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-ui-production-readiness/07-CONTEXT.md
@.planning/phases/07-web-ui-production-readiness/07-RESEARCH.md
@.planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md

# All UI + backend deliverables this deploy validates
@.planning/phases/07-web-ui-production-readiness/07-04-SUMMARY.md
@.planning/phases/07-web-ui-production-readiness/07-05-SUMMARY.md
@.planning/phases/07-web-ui-production-readiness/07-06-SUMMARY.md
@.planning/phases/07-web-ui-production-readiness/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document + wire the staging deployment env (CORS_ORIGINS + NEXT_PUBLIC_API_URL) (D-06)</name>
  <files>.env.example, docs/deployment.md</files>
  <read_first>
    - docs/deployment.md (existing deployment guide to extend with a staging section)
    - .env.example (current env keys incl. CORS_ORIGINS; add staging examples)
    - docker-compose.prod.yml (the `web` service build args + env; the `NEXT_PUBLIC_API_URL` build ARG and `API_URL` server var — the server/client base-URL split)
    - web/Dockerfile (l.12-15 — NEXT_PUBLIC_API_URL is a build ARG baked into client bundles, per Pitfall 3)
    - api/app/auth/routes.py (`_validate_web_redirect_uri` l.83-112 — allowlists redirect host against CORS_ORIGINS, fails closed)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (D-06; Pitfalls 2 & 3; Environment Availability — no .env.production.example, derive from .env.example + prod compose)
  </read_first>
  <action>
    Add a "Staging deploy" section to docs/deployment.md covering: (1) the staging web origin MUST be added
    to CORS_ORIGINS or `_validate_web_redirect_uri` fails closed and every login + the D-04 merge redirect
    400s (Pitfall 2); (2) the staging web image is a DISTINCT build with its own `NEXT_PUBLIC_API_URL` build
    arg (baked at build time, not runtime-overridable, Pitfall 3) pointing at the staging API host, while the
    server-side `API_URL` uses the internal service URL; (3) the redshirt proxy + DNS + TLS are external host
    infra prerequisites (see user_setup). Add commented staging example entries to .env.example (staging
    CORS_ORIGINS value, staging NEXT_PUBLIC_API_URL). Do NOT edit docker-compose.prod.yml destructively — the
    prod stack is Phase 8's apex; document the staging build/run invocation instead. Choose the exact staging
    hostname (Claude's discretion per CONTEXT, e.g. staging.oviddb.org / api.staging.oviddb.org) and use it
    consistently.
  </action>
  <verify>
    <automated>grep -c 'staging' docs/deployment.md && grep -Ec 'CORS_ORIGINS|NEXT_PUBLIC_API_URL' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - docs/deployment.md has a staging section naming the CORS_ORIGINS requirement + the distinct NEXT_PUBLIC_API_URL build arg + the redshirt/DNS/TLS prerequisites
    - .env.example contains staging example values for CORS_ORIGINS and NEXT_PUBLIC_API_URL (commented)
    - `grep -c 'staging' docs/deployment.md` ≥ 1 and `grep -Ec 'CORS_ORIGINS|NEXT_PUBLIC_API_URL' .env.example` ≥ 2
    - docker-compose.prod.yml is unchanged (no destructive prod edit)
  </acceptance_criteria>
  <done>The staging env (CORS_ORIGINS allowlist + distinct NEXT_PUBLIC_API_URL build) and its external infra prerequisites are documented and exemplified.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full web + api test suites green</name>
  <files>docs/deployment.md</files>
  <read_first>
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (Validation Architecture — Sampling Rate: phase gate = full web + api suites)
    - api/tests/ (the ~442 api tests; run under the api venv)
    - web/src/__tests__/ (the web Vitest suite incl. the new primitives/settings + extended disc-detail/pages/submit tests)
  </read_first>
  <action>
    Run the full phase gate and record the result (counts) in the docs/deployment.md staging section's
    "verified state" note: the complete web Vitest suite and the complete api pytest suite must both be green
    (the two pre-existing api third-party deprecation warnings noted in the venv memory are expected and are
    NOT repo defects — do not treat them as failures, but confirm no NEW warnings were introduced by this
    phase). If anything is red, fix it inline before proceeding (never wave off).
  </action>
  <verify>
    <automated>cd web && npm test && cd ../api && .venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes (full Vitest suite, including primitives/settings/disc-detail/pages/submit)
    - `cd api && .venv/bin/python -m pytest tests/ -q` passes (full api suite; only the two known pre-existing third-party deprecation warnings, no new ones)
    - The green phase-gate result (suite counts) is recorded in docs/deployment.md
  </acceptance_criteria>
  <done>Both full suites are green and the result is recorded as the phase gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the staging deploy end-to-end + D-03 accessibility sign-off (D-06 / D-03 blocking gate)</name>
  <action>After the external staging infra is in place (DNS + redshirt TLS route + staging web origin in CORS_ORIGINS + a staging web image built with the staging NEXT_PUBLIC_API_URL), build and serve the staging web container, then walk the developer through the five verification checks below over HTTPS. Block on any failure; fix inline and re-verify. This is the D-06 "live on staging" confirmation and the D-03 blocking accessibility gate.</action>
  <files>(no files modified — live staging verification + a11y sign-off)</files>
  <what-built>
    The Phase 7 web UI (search anchor + primitives, disc-detail fingerprint aliases + withheld message,
    submit + set/chapter a11y parity, settings add/remove + enumeration-safe merge banner) plus the staging
    deployment env docs, ready to build + serve the staging web image behind redshirt over TLS.
  </what-built>
  <how-to-verify>
    Prerequisite (external infra, see user_setup): staging DNS + redshirt TLS route for the staging web +
    API hosts; the staging web origin added to the API's CORS_ORIGINS; a staging web image built with the
    staging NEXT_PUBLIC_API_URL build arg.
    Then, on the live staging URL over HTTPS:
    1. Search: enter a title; confirm the input is the centered focal anchor, results/count/pagination render, and the empty-state hint is legible (AA contrast) in BOTH light and dark.
    2. Disc detail: open a disc; confirm the Fingerprint aliases section lists ALL identity strings with the primary badged (data-testid="fingerprint-aliases"); open an unverified disc and confirm the "Structure withheld until a second contributor verifies this disc." message.
    3. Submit: sign in, upload an `ovid fingerprint --json`, confirm preview + submit success; confirm the set-toggle and fields are keyboard-operable (Tab/Space/Enter) with visible focus rings.
    4. Settings: confirm "Link a provider" initiates the add flow; unlink a provider (min-one stays disabled with the correct copy); trigger an email-conflict and confirm the merge banner is styled (not raw JSON on the API host), names only your current providers, offers a re-auth link, and leaks no email/account id (D-04 redirect + ME-02).
    5. Accessibility (D-03 blocking): tab through every interactive element on all four surfaces — each shows a visible focus-visible ring; Escape dismisses any open dropdown/dialog; verify WCAG AA contrast (4.5:1 text, 3:1 UI) in BOTH themes.
  </how-to-verify>
  <resume-signal>Type "approved" once staging is live and all five checks (incl. the D-03 a11y floor) pass in both themes, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → staging web (redshirt/TLS) | Untrusted traffic reaches the staging Next server |
| staging web → staging API | Cross-origin browser + server calls; CORS_ORIGINS + web_redirect_uri allowlist govern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08-01 | Tampering | open redirect / JWT exfiltration on staging login | high | mitigate | Staging web origin explicitly added to CORS_ORIGINS so `_validate_web_redirect_uri` allowlists it (fail-closed otherwise); no wildcard. Documented as the required staging step. |
| T-07-08-02 | Information Disclosure | staging exposing a near-empty catalog publicly | medium | mitigate | Staging is a non-apex preview URL; the public oviddb.org apex cutover + seeding are deferred to Phase 8 (D-06), avoiding public exposure of an empty catalog. |
| T-07-08-03 | Spoofing | TLS/DNS misconfig on staging host | medium | transfer | TLS + DNS + redshirt are external host infra (user_setup); the human-verify checkpoint confirms HTTPS + routing before sign-off. |
| T-07-08-04 | Tampering | supply chain (new deps) | low | accept | No packages installed this phase (D-02); package legitimacy gate N/A. Staging build uses the existing pinned images. |
</threat_model>

<verification>
- `cd web && npm test` and `cd api && .venv/bin/python -m pytest tests/ -q` both green (phase gate).
- `grep -c 'staging' docs/deployment.md` ≥ 1; `grep -Ec 'CORS_ORIGINS|NEXT_PUBLIC_API_URL' .env.example` ≥ 2.
- Human-verify checkpoint approved: staging live over TLS with all five checks + the D-03 a11y floor passing in both themes.
</verification>

<success_criteria>
Phase 7 is verifiably deployable and live on a staging URL over TLS with search, disc detail + aliases,
submit, and settings add/remove + merge redirect all working, the D-03 accessibility floor verified in both
themes, and both full test suites green.
</success_criteria>

## Artifacts this phase produces (07-08)
- `docs/deployment.md` — new "Staging deploy" section (CORS_ORIGINS allowlist, distinct NEXT_PUBLIC_API_URL build, redshirt/DNS/TLS prerequisites, verified phase-gate state)
- `.env.example` — commented staging CORS_ORIGINS + NEXT_PUBLIC_API_URL example entries
- (no code artifacts — this is deploy/config/docs + human verification)

<output>
Create `.planning/phases/07-web-ui-production-readiness/07-08-SUMMARY.md` when done.
</output>
