---
phase: 07-web-ui-production-readiness
plan: 06
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/components/SubmitForm.tsx
  - web/components/SetSearchInput.tsx
  - web/components/ChapterEditor.tsx
  - web/src/__tests__/submit.test.tsx
autonomous: true
requirements: [WEBUI-03]
must_haves:
  truths:
    - "The authenticated submit flow still works end-to-end: parse fingerprint JSON, preview, release+disc fields, multi-disc-set toggle + SetSearchInput, per-title ChapterEditor, submit → success/redirect (WEBUI-03)"
    - "Submit controls migrate to the Button/Input/Field primitives and gain the D-03 focus-visible ring"
    - "parse-error and submit-error surface via aria-live regions (D-03), keeping their data-testid values"
    - "SetSearchInput and ChapterEditor reach D-02 token / D-03 a11y parity (no 12px tier, no sub-4px spacing, keyboard-operable) — the D-01 R-4 re-review fix"
  artifacts:
    - web/src/__tests__/submit.test.tsx
  key_links:
    - "SubmitForm → submitDisc() → POST /v1/disc (Bearer) (unchanged contract)"
    - "set-toggle + SetSearchInput ↔ create/link set flow; ChapterEditor ↔ per-title chapter payload"
---

<!-- planner-discipline-allow: text-xs -->

<objective>
Bring the submit surface (WEBUI-03) — already functionally complete — to the D-02 token layer and D-03
accessibility floor, and complete the D-01 re-review UX-parity fix (R-4) for the re-integrated
`SetSearchInput` and `ChapterEditor`. The set/chapter sub-UI predates the token layer: raw `text-xs`
(12px, banned), raw `focus:`/no focus-visible, sub-4px `py-1`/`py-0.5` vestiges, and bespoke `inputClass`
strings that should adopt the shared primitives.

Purpose: WEBUI-03's gap is a11y/token polish + the re-review of the set/chapter authoring UX. The submit
API contract (`POST /v1/disc`, Bearer) is unchanged; this is polish + keyboard operability + aria-live.
Output: primitive-backed, keyboard-operable submit + set/chapter authoring components + extended tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-ui-production-readiness/07-RESEARCH.md
@.planning/phases/07-web-ui-production-readiness/07-PATTERNS.md
@.planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md
@web/AGENTS.md

@.planning/phases/07-web-ui-production-readiness/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate SubmitForm to primitives + aria-live errors, preserving the submit contract (WEBUI-03)</name>
  <files>web/components/SubmitForm.tsx</files>
  <read_first>
    - web/components/SubmitForm.tsx (file-upload of `ovid fingerprint --json`, `data-testid="parse-error"`, preview card, release/disc fields, multi-disc-set toggle, per-title ChapterEditor, submit + success/redirect; the custom peer-checkbox set-toggle)
    - web/lib/api.ts (`submitDisc()` signature, `ApiError{status,code}`, Bearer usage)
    - web/components/Button.tsx / Input.tsx / Field.tsx (07-01 primitives)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (SC-3 GAP list: focus-visible, aria-live on submit-error/parse-error, set-toggle keyboard operability)
    - .planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md (Copywriting: "Submit disc" CTA; Focus/aria-live; Typography/Spacing)
    - web/node_modules/next/dist/docs/ (client-component guidance — Next 16.2.2 ≠ training data)
  </read_first>
  <behavior>
    - File input, release/disc fields, and the submit CTA ("Submit disc") use the 07-01 Input/Field/Button primitives; the submit CTA carries the focus-visible ring.
    - `parse-error` and `submit-error` regions carry `aria-live="polite"` and keep their existing `data-testid` values.
    - The multi-disc-set toggle is keyboard-operable (Tab focus + Space toggles) with a visible focus-visible state.
    - The end-to-end contract is unchanged: valid fingerprint JSON parses to a preview, submit calls `submitDisc()` → `POST /v1/disc` (Bearer), success redirects; auth-gate + parse-error paths behave as before.
  </behavior>
  <action>
    Migrate web/components/SubmitForm.tsx's raw controls to the 07-01 Input/Field/Button primitives; label
    the primary CTA "Submit disc". Add `aria-live="polite"` to the `parse-error` and `submit-error`
    regions, keeping their `data-testid`s. Make the multi-disc-set toggle keyboard-operable (native
    checkbox semantics or role+key handlers) with a focus-visible state — replace the mouse-noisy
    peer-checkbox focus. Fold `text-xs`→`text-sm` and remove sub-4px spacing. Do NOT change the
    `submitDisc()` call, the payload shape, the parse logic, or the success/redirect behavior.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/submit.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SubmitForm uses the Input/Field/Button primitives; the CTA reads "Submit disc"
    - `parse-error` and `submit-error` regions have `aria-live="polite"` and retain their `data-testid`s
    - The set-toggle is reachable by Tab and togglable by keyboard with a focus-visible state
    - `grep -c 'text-xs' web/components/SubmitForm.tsx` returns 0
    - `cd web && npx vitest run src/__tests__/submit.test.tsx` passes (happy-path + parse-error + auth-gate preserved)
  </acceptance_criteria>
  <done>The submit form is primitive-backed, keyboard-operable, and aria-live, with its submit contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: D-01 R-4 parity for SetSearchInput + ChapterEditor</name>
  <files>web/components/SetSearchInput.tsx, web/components/ChapterEditor.tsx</files>
  <read_first>
    - web/components/SetSearchInput.tsx (has `role="combobox"`/keyboard nav/`aria-selected` already; raw `text-xs` + bespoke `inputClass` to migrate)
    - web/components/ChapterEditor.tsx (per-title chapter authoring; raw focus:/text-xs/sub-4px vestiges per RESEARCH R-4)
    - web/components/Input.tsx / Button.tsx (primitives to adopt; SetSearchInput's combobox input should adopt the Input primitive's focus-visible ring while keeping its ARIA roles)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (Re-review §4 UX parity — the exact vestige list for these two files)
    - .planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md (Spacing zero sub-4px; Typography no 12px; Focus)
  </read_first>
  <action>
    Bring SetSearchInput.tsx and ChapterEditor.tsx to D-02/D-03 parity (D-01 R-4): fold `text-xs`→`text-sm`;
    replace sub-4px spacing with 4px-multiple tokens; migrate bespoke `inputClass` strings to the shared
    Input primitive (preserving SetSearchInput's `role="combobox"`, keyboard nav, and `aria-selected`);
    ensure every interactive element has a `focus-visible:` ring; keep all `data-testid`s. Do not change the
    set-search API call or the chapter payload shape — styling/a11y parity only.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/submit.test.tsx && grep -c 'text-xs' web/components/SetSearchInput.tsx web/components/ChapterEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'text-xs' web/components/SetSearchInput.tsx` and `.../ChapterEditor.tsx` both return 0
    - No sub-4px spacing token remains; interactive elements carry a focus-visible ring
    - SetSearchInput keeps `role="combobox"` + `aria-selected` + keyboard nav; all `data-testid`s intact
    - `cd web && npx vitest run src/__tests__/submit.test.tsx` passes
  </acceptance_criteria>
  <done>SetSearchInput and ChapterEditor meet the token/type/spacing/focus contract with their behavior and ARIA intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend submit tests — primitive focus-visible + aria-live + set-toggle keyboard (WEBUI-03 a11y)</name>
  <files>web/src/__tests__/submit.test.tsx</files>
  <read_first>
    - web/src/__tests__/submit.test.tsx (existing happy-path/parse-error/auth-gate tests + `vi.mock("@/lib/api")` pattern; `user-event` usage)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (Validation Architecture — WEBUI-03 "extend a11y"; D-03 keyboard row)
  </read_first>
  <behavior>
    - The submit CTA carries a focus-visible ring class and the "Submit disc" label.
    - `parse-error`/`submit-error` regions expose `aria-live="polite"`.
    - The multi-disc-set toggle is focusable and togglable via keyboard (`user-event` Tab + Space).
    - Existing happy-path submit, parse-error surfacing, and auth-gate assertions still pass.
  </behavior>
  <action>
    Extend web/src/__tests__/submit.test.tsx with assertions for the focus-visible CTA, the aria-live error
    regions, and keyboard operability of the set-toggle (`user-event`). Keep the wholesale-mock,
    data-testid, behavior-assert style. RED partner to Tasks 1-2.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/submit.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - submit.test.tsx asserts focus-visible CTA, aria-live error regions, and keyboard set-toggle, plus the preserved submit/parse/auth behavior
    - `cd web && npx vitest run src/__tests__/submit.test.tsx` passes
  </acceptance_criteria>
  <done>Submit a11y + preserved behavior are proven by an automated Vitest run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded fingerprint JSON → client parse | Untrusted local file parsed in the browser |
| authenticated client → POST /v1/disc | Bearer-gated write; payload validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Tampering | XSS via parsed JSON / preview render | medium | mitigate | Parsed values rendered as JSX text; React auto-escapes; parse errors surface via `parse-error` (no `dangerouslySetInnerHTML`). |
| T-07-06-02 | Input Validation | submit payload / chapter counts | low | accept | Server-side Pydantic + explicit checks (≤999 chapters, unique index) unchanged; client parse-error path preserved. |
| T-07-06-03 | Denial of Service | authenticated submit writes | low | accept | `POST /v1/disc` already carries the stacked AUTH_WRITE_LIMIT; this plan does not alter it (set-creation ceiling handled in 07-03). |
</threat_model>

<verification>
- `cd web && npx vitest run src/__tests__/submit.test.tsx` passes.
- `cd web && grep -c 'text-xs' components/SubmitForm.tsx components/SetSearchInput.tsx components/ChapterEditor.tsx` == 0.
- `cd web && npm test` full suite green.
</verification>

<success_criteria>
The submit form and its set/chapter sub-UI are primitive-backed, keyboard-operable, and aria-live at the
D-02/D-03 floor, with the `POST /v1/disc` submit contract fully preserved — proven by an automated Vitest run.
</success_criteria>

## Artifacts this phase produces (07-06)
- `web/components/SubmitForm.tsx` — primitive-backed controls, aria-live errors, keyboard set-toggle, "Submit disc" CTA (no contract change)
- `web/components/SetSearchInput.tsx` / `web/components/ChapterEditor.tsx` — D-01 R-4 token/a11y parity (behavior + ARIA preserved)
- `web/src/__tests__/submit.test.tsx` — extended a11y assertions

<output>
Create `.planning/phases/07-web-ui-production-readiness/07-06-SUMMARY.md` when done.
</output>
