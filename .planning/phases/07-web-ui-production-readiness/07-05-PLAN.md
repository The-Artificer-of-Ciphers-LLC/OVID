---
phase: 07-web-ui-production-readiness
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/app/page.tsx
  - web/components/SearchForm.tsx
  - web/src/__tests__/pages.test.tsx
autonomous: true
requirements: [WEBUI-01]
must_haves:
  truths:
    - "The search input is the focal anchor per UI-SPEC — centered, the widest control, with the accent 'Search discs' CTA (WEBUI-01)"
    - "SearchForm's controls migrate to the Button/Input/Field primitives so they inherit the D-03 focus-visible ring"
    - "The no-search-yet empty-state hint meets WCAG AA contrast (neutral-400 → neutral-500 if borderline) in both themes"
    - "Existing search behavior is preserved: query push (?q=&year=), result grid, count, pagination, zero-results and no-query empty states"
  artifacts:
    - web/src/__tests__/pages.test.tsx
  key_links:
    - "SearchForm ?q=&year= push ↔ page.tsx server fetch searchReleases() → GET /v1/search (unchanged)"
    - "SearchForm controls ↔ 07-01 Button/Input/Field primitives"
---

<!-- planner-discipline-allow: text-xs -->
<!-- planner-discipline-allow: text-neutral-400 -->

<objective>
Raise the search surface (WEBUI-01) — already functionally complete — to the UI-SPEC visual hierarchy and
D-03 accessibility floor: make the search input the centered focal anchor with the accent "Search discs"
CTA, migrate SearchForm's controls onto the 07-01 primitives (inheriting the focus-visible ring), and fix
the borderline empty-state contrast. No behavior change to the search path.

Purpose: WEBUI-01 is "already working"; the phase gap is polish + a11y + (staging) deploy. This plan closes
the polish/a11y half; 07-08 covers deploy.
Output: an anchored, primitive-backed, AA-contrast search surface + extended page tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-ui-production-readiness/07-RESEARCH.md
@.planning/phases/07-web-ui-production-readiness/07-PATTERNS.md
@.planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md
@web/AGENTS.md

@.planning/phases/07-web-ui-production-readiness/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor the search input + migrate SearchForm to primitives + fix empty-state contrast (WEBUI-01)</name>
  <files>web/app/page.tsx, web/components/SearchForm.tsx</files>
  <read_first>
    - web/app/page.tsx (server component: `searchReleases()` fetch, DiscCard grid, result count, pagination, zero-results "No releases found.", no-query empty state; the `text-neutral-400` empty-state hint flagged borderline)
    - web/components/SearchForm.tsx (l.25-72 — the Suspense-wrapped useSearchParams form; the input + button to migrate; current compact inline row layout)
    - web/components/Button.tsx / Input.tsx / Field.tsx (07-01 primitives to adopt)
    - .planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md (Visual Hierarchy: search input centered/widest, accent CTA; Copywriting: "Search discs", empty-state copy; Color: neutral-400 contrast target)
    - .planning/phases/07-web-ui-production-readiness/07-PATTERNS.md (search polish notes: focal anchor, focus-visible, neutral-400→neutral-500)
    - web/node_modules/next/dist/docs/ (App Router + Suspense/useSearchParams guidance — Next 16.2.2 ≠ training data)
  </read_first>
  <behavior>
    - The search input renders centered and as the widest control on the page; the submit CTA reads "Search discs" and uses the accent primary Button.
    - SearchForm's input + submit use the 07-01 Input/Button primitives (inheriting the focus-visible ring); useSearchParams stays inside its `<Suspense>` boundary.
    - Submitting still pushes `?q=` (+ optional year/type) and the server page fetches + renders the DiscCard grid, result count, and pagination exactly as before.
    - The no-search-yet hint renders the UI-SPEC copy "Enter a title to search the database." at AA contrast (neutral-500 or darker, not neutral-400).
    - Zero-results still renders "No releases found." + "Check the spelling or try a broader title."
  </behavior>
  <action>
    Migrate web/components/SearchForm.tsx's raw `<input>`/`<button>` to the 07-01 Input + Button (primary,
    accent) primitives with the "Search discs" label; keep the Suspense-wrapped `useSearchParams` and the
    existing `?q=`/year push logic unchanged. In web/app/page.tsx, restructure the search area so the input
    is the centered, widest focal control per UI-SPEC (not the current compact inline row); keep the
    server-side `searchReleases()` fetch, DiscCard grid, result count, and pagination as-is. Update the
    no-query empty-state hint to the UI-SPEC copy and darken `text-neutral-400` to `text-neutral-500` (or
    verify AA and darken if borderline) so it clears 4.5:1 in both themes. Fold any `text-xs` to `text-sm`.
    Do not change the API call or query-param contract.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/pages.test.tsx && grep -c 'text-neutral-400' web/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SearchForm uses the Input + Button primitives; the CTA text is "Search discs"; useSearchParams stays inside `<Suspense>`
    - `grep -c 'text-neutral-400' web/app/page.tsx` returns 0 (borderline empty-state contrast darkened)
    - The no-query and zero-results empty states render the exact UI-SPEC copy
    - Search still pushes `?q=` and renders results/count/pagination (existing pages tests pass)
    - `cd web && npx vitest run src/__tests__/pages.test.tsx` passes
  </acceptance_criteria>
  <done>The search input is the anchored, primitive-backed focal control with an accent CTA and AA-contrast empty state; search behavior is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pages tests — focus-visible + empty-state copy/contrast + preserved search behavior</name>
  <files>web/src/__tests__/pages.test.tsx</files>
  <read_first>
    - web/src/__tests__/pages.test.tsx (existing search render/zero/no-query/pagination assertions to extend)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (Validation Architecture — WEBUI-01 "extend for a11y")
  </read_first>
  <behavior>
    - The rendered SearchForm submit control carries a `focus-visible:` ring class (via the Button primitive).
    - The no-query empty state renders the exact UI-SPEC hint copy; the hint element is not `text-neutral-400`.
    - Existing behavior assertions (results grid, zero-results copy, pagination) still hold.
  </behavior>
  <action>
    Extend web/src/__tests__/pages.test.tsx with assertions for: the submit CTA carrying a focus-visible
    ring class + the "Search discs" label, the no-query empty-state copy, and continued rendering of
    results/zero-results/pagination. Keep the wholesale `vi.mock` + data-testid style. RED partner to Task 1.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/pages.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - pages.test.tsx asserts focus-visible on the CTA, the "Search discs" label, and the empty-state copy, plus the preserved result/pagination behavior
    - `cd web && npx vitest run src/__tests__/pages.test.tsx` passes
  </acceptance_criteria>
  <done>Search a11y + copy + preserved behavior are proven by an automated Vitest run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → server search fetch | `q`/`year` params cross from the browser to `GET /v1/search` |
| API results → rendered grid | Release titles/metadata rendered to the reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | XSS via search results / query echo | medium | mitigate | Results and echoed query rendered as JSX text; React auto-escapes; no `dangerouslySetInnerHTML`. |
| T-07-05-02 | Input Validation | search `q`/`year` | low | accept | Server-side validation unchanged (empty `q` → 400 handled by the API); this plan does not alter the contract. |
| T-07-05-03 | Information Disclosure | empty-state contrast | low | accept | Cosmetic/a11y only; no data path touched. |
</threat_model>

<verification>
- `cd web && npx vitest run src/__tests__/pages.test.tsx` passes.
- `cd web && grep -c 'text-neutral-400' app/page.tsx` == 0; `grep -c 'text-xs' app/page.tsx components/SearchForm.tsx` == 0.
- `cd web && npm test` full suite green.
</verification>

<success_criteria>
The search input is the centered focal anchor with an accent "Search discs" CTA and primitive-backed
focus-visible controls, the empty-state hint clears AA contrast, and all existing search behavior is
preserved — proven by an automated Vitest run.
</success_criteria>

## Artifacts this phase produces (07-05)
- `web/components/SearchForm.tsx` — migrated to Button/Input primitives; "Search discs" CTA (no new props)
- `web/app/page.tsx` — anchored search layout + AA-contrast empty-state copy
- `web/src/__tests__/pages.test.tsx` — extended a11y/copy assertions

<output>
Create `.planning/phases/07-web-ui-production-readiness/07-05-SUMMARY.md` when done.
</output>
