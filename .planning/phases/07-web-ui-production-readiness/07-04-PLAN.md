---
phase: 07-web-ui-production-readiness
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-03"]
files_modified:
  - web/app/disc/[fingerprint]/page.tsx
  - web/components/SiblingDiscs.tsx
  - web/components/ChapterList.tsx
  - web/src/__tests__/disc-detail.test.tsx
autonomous: true
requirements: [WEBUI-02]
must_haves:
  truths:
    - "The disc detail page renders every fingerprint alias (all identity strings — dvd1-*, dvdread1-*, BD tiers) with the primary marked, never hidden or renumbered (WEBUI-02 + compatibility guardrail)"
    - "When a disc is unverified (titles redacted to []), the page shows an explicit 'Structure withheld until a second contributor verifies this disc.' message instead of a generic empty state (Pitfall 4)"
    - "The no-aliases empty state shows the UI-SPEC copy 'No additional fingerprint aliases recorded.'"
    - "SiblingDiscs and ChapterList read parity with the D-02 token layer + D-03 focus floor (no 12px tier, focus-visible on interactive elements)"
  artifacts:
    - web/src/__tests__/disc-detail.test.tsx
  key_links:
    - "getDisc() response fingerprint_aliases[] ↔ the new aliases <section> on disc/[fingerprint]/page.tsx"
    - "disc.status === 'unverified' ↔ the withheld-structure message branch"
  prohibitions:
    - statement: "No identity string is hidden or renumbered — all fingerprint_aliases render (dvd1-* stays resolvable/visible)"
      status: resolved
      verification: "disc-detail.test.tsx asserts all provided aliases render, primary badged"
---

<!-- planner-discipline-allow: text-xs -->

<objective>
Deliver the single biggest requirement gap in the phase: **render fingerprint aliases on the disc detail
page (WEBUI-02)**. `getDisc()` already returns `fingerprint_aliases[]` (primary-first, populated by the
backend) but the page never iterates them — it prints `disc.fingerprint` once and stops. Add the aliases
section, the unverified-withheld message (Pitfall 4), and bring the sibling/chapter read components to the
D-02/D-03 floor.

Purpose: WEBUI-02 explicitly requires the detail view to "show fingerprint aliases." This is a
server-component render of data already present — no new fetch — plus production-polish for the redacted
and no-alias states.
Output: aliases `<section>` + withheld message on the detail page, token/a11y parity on SiblingDiscs +
ChapterList, and extended disc-detail tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-ui-production-readiness/07-RESEARCH.md
@.planning/phases/07-web-ui-production-readiness/07-PATTERNS.md
@.planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md
@web/AGENTS.md

# Foundation this plan consumes
@.planning/phases/07-web-ui-production-readiness/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the fingerprint-aliases section + unverified-withheld message on the disc detail page (WEBUI-02)</name>
  <files>web/app/disc/[fingerprint]/page.tsx</files>
  <read_first>
    - web/app/disc/[fingerprint]/page.tsx (l.133-160 — the fingerprint block + SiblingDiscs usage; where aliases are NEVER iterated today; the current "No title structure available." path)
    - web/lib/api.ts (`getDisc()` return type; `FingerprintAlias { fingerprint, method, is_primary }` shape; `DiscLookupResponse.status`)
    - .planning/phases/07-web-ui-production-readiness/07-PATTERNS.md (`web/app/disc/[fingerprint]/page.tsx` section — RESEARCH Pattern 2 alias markup, withheld-message rule, text-xs→text-sm fold)
    - .planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md (Copywriting: no-aliases + withheld copy; Typography: no 12px tier)
    - web/node_modules/next/dist/docs/ (App Router server-component + async params guidance — per web/AGENTS.md, Next 16.2.2 ≠ training data)
  </read_first>
  <behavior>
    - Given `disc.fingerprint_aliases.length > 1`, an aliases `<section>` renders each alias as a Geist-Mono `<code>` with its `method` label; the alias with `is_primary` shows a "primary" status badge (info palette). Marked with `data-testid="fingerprint-aliases"`.
    - Given `disc.fingerprint_aliases.length <= 1`, the section shows "No additional fingerprint aliases recorded." (UI-SPEC copy).
    - All provided identity strings render — none hidden or renumbered (dvd1-* + dvdread1-* both visible).
    - Given `disc.status === "unverified"` (titles === []), the structure area shows "Structure withheld until a second contributor verifies this disc." instead of the generic empty text; aliases + release still render.
  </behavior>
  <action>
    In web/app/disc/[fingerprint]/page.tsx (server component — data already in `disc`, no new fetch), insert
    a fingerprint-aliases `<section data-testid="fingerprint-aliases">` near the existing fingerprint block
    (l.133-149). Iterate `disc.fingerprint_aliases`: render each `fingerprint` in Geist Mono (`font-mono
    break-all`), its `method`, and a "primary" badge using the info status palette
    (`bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-200`) when `is_primary`. When there are no
    additional aliases, render the UI-SPEC copy "No additional fingerprint aliases recorded." Replace the
    generic "No title structure available." path: when `disc.status === "unverified"`, render "Structure
    withheld until a second contributor verifies this disc." Fold the fingerprint block's `text-xs` up to
    `text-sm` (no 12px tier). Keep the existing SEO metadata and `notFound()` on 404. Consult the installed
    Next docs before touching server-component/async-params code.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/disc-detail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `web/app/disc/[fingerprint]/page.tsx` renders a `data-testid="fingerprint-aliases"` region iterating `disc.fingerprint_aliases`
    - The primary alias shows a "primary" badge; every provided alias string appears
    - `disc.status === "unverified"` renders the exact withheld copy; the no-aliases empty state renders the exact UI-SPEC copy
    - `grep -c 'text-xs' web/app/disc/[fingerprint]/page.tsx` returns 0 (folded to text-sm)
    - `cd web && npx vitest run src/__tests__/disc-detail.test.tsx` passes
  </acceptance_criteria>
  <done>The detail page shows all fingerprint aliases with the primary marked, an explicit withheld-structure message for unverified discs, and the correct empty-state copy.</done>
</task>

<task type="auto">
  <name>Task 2: Bring SiblingDiscs + ChapterList to the D-02 token / D-03 a11y floor</name>
  <files>web/components/SiblingDiscs.tsx, web/components/ChapterList.tsx</files>
  <read_first>
    - web/components/SiblingDiscs.tsx (current sibling summary render — raw text-xs / focus: usages)
    - web/components/ChapterList.tsx (expandable chapter render used on disc detail; `px-2 py-1` sub-rhythm noted in RESEARCH R-4)
    - web/components/Button.tsx + web/components/Input.tsx (the 07-01 primitives to adopt for any interactive control)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (Re-review §4 UX parity — the raw text-xs / focus / sub-4px vestiges list)
    - .planning/phases/07-web-ui-production-readiness/07-UI-SPEC.md (Spacing: zero sub-4px; Typography: no 12px tier; Focus block)
  </read_first>
  <action>
    Migrate SiblingDiscs.tsx and ChapterList.tsx to the token layer + a11y floor (D-01 R-4 UX parity, D-02,
    D-03): fold every `text-xs` up to `text-sm`; replace any sub-4px spacing (`py-0.5`, `mt-0.5`) with
    4px-multiple tokens per UI-SPEC Spacing; replace mouse-noisy `focus:` on any interactive element
    (expand/collapse toggles, links) with the shared primitives or the `focus-visible:` ring; keep every
    existing `data-testid`. Do not change the data shown (the R-1 backend redaction in 07-03 governs which
    sibling fields exist) — this task is styling/a11y parity only.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/disc-detail.test.tsx && grep -c 'text-xs' web/components/SiblingDiscs.tsx web/components/ChapterList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'text-xs' web/components/SiblingDiscs.tsx` and `.../ChapterList.tsx` both return 0
    - No sub-4px spacing token (`py-0.5`, `mt-0.5`, `px-0.5`) remains in either file
    - Interactive elements carry a `focus-visible:` ring (via primitive or class); all prior `data-testid`s intact
    - `cd web && npx vitest run src/__tests__/disc-detail.test.tsx` passes
  </acceptance_criteria>
  <done>SiblingDiscs and ChapterList match the production token/type/spacing/focus contract without changing their data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend disc-detail tests — alias rendering + withheld message (WEBUI-02 Wave 0 gap)</name>
  <files>web/src/__tests__/disc-detail.test.tsx</files>
  <read_first>
    - web/src/__tests__/disc-detail.test.tsx (existing structure; wholesale `vi.mock("@/lib/api")` pattern)
    - .planning/phases/07-web-ui-production-readiness/07-RESEARCH.md (Validation Architecture — WEBUI-02 alias + unverified-withheld rows are the ❌ Wave 0 gap)
    - web/lib/api.ts (FingerprintAlias + DiscLookupResponse shapes to build fixtures)
  </read_first>
  <behavior>
    - A disc with multiple `fingerprint_aliases` renders each alias string, its method, and a "primary" badge on the primary one, queried via `data-testid="fingerprint-aliases"`.
    - A disc with `status: "unverified"` and `titles: []` renders the exact withheld-structure copy and does NOT render a titles table.
    - A disc with only one alias renders the "No additional fingerprint aliases recorded." copy.
  </behavior>
  <action>
    Extend web/src/__tests__/disc-detail.test.tsx (Vitest + Testing Library, `vi.mock("@/lib/api")`
    wholesale, DOM by `data-testid`). Add fixtures + assertions for: multi-alias rendering with the primary
    badge and all identity strings present; the unverified-withheld message with no titles table; the
    no-additional-aliases empty copy. These are the RED partners for Task 1.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/__tests__/disc-detail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - disc-detail.test.tsx asserts alias rendering (all strings + primary badge), unverified-withheld copy, and no-aliases copy
    - `cd web && npx vitest run src/__tests__/disc-detail.test.tsx` passes
  </acceptance_criteria>
  <done>Alias rendering and the withheld state are proven by an automated Vitest run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → server-rendered page | Disc identity strings + structural data are rendered to any public reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | unverified disc detail | high | mitigate | The page renders only what the API returns; unverified discs already return `titles: []` (D-09) and R-1 (07-03) redacts sibling structural fields. The new withheld message states the redaction rather than exposing data. Aliases are non-sensitive identity strings. |
| T-07-04-02 | Tampering | XSS via disc/release/alias strings | medium | mitigate | All values rendered as JSX text; React auto-escapes; no `dangerouslySetInnerHTML`. |
| T-07-04-03 | Spoofing | alias renumbering / hiding | medium | mitigate | Render ALL `fingerprint_aliases` verbatim, primary-first; never hide or renumber (dvd1-* stability guardrail). Test asserts completeness. |
</threat_model>

<verification>
- `cd web && npx vitest run src/__tests__/disc-detail.test.tsx` passes.
- `cd web && grep -c 'text-xs' app/disc/[fingerprint]/page.tsx components/SiblingDiscs.tsx components/ChapterList.tsx` == 0.
- `cd web && npm test` full suite remains green.
</verification>

<success_criteria>
The disc detail page renders all fingerprint aliases (primary marked), shows an explicit withheld message
for unverified discs and the correct no-aliases copy, and SiblingDiscs/ChapterList meet the token/a11y
floor — all proven by an automated Vitest run.
</success_criteria>

## Artifacts this phase produces (07-04)
- `web/app/disc/[fingerprint]/page.tsx` — new `data-testid="fingerprint-aliases"` section + unverified-withheld message branch (WEBUI-02)
- `web/components/SiblingDiscs.tsx` / `web/components/ChapterList.tsx` — token/a11y parity (no new props)
- `web/src/__tests__/disc-detail.test.tsx` — extended with alias + withheld assertions

<output>
Create `.planning/phases/07-web-ui-production-readiness/07-04-SUMMARY.md` when done.
</output>
