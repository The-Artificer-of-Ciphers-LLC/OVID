---
phase: 02-two-contributor-verification-workflow
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - docs/api-reference.md
  - docs/docker-quickstart.md
  - docs/OVID-technical-spec.md
  - docs/contributing.md
  - docs/privacy.md
  - .env.example
autonomous: true
requirements: [VERIFY-04]
must_haves:
  truths:
    - "No published doc advertises POST /v1/disc/{fingerprint}/verify as a live endpoint (D-02); the contributing guide describes confirmation as an independent re-submission via the ovid client, not a bodyless verify call."
    - "The IP-hash data category is disclosed in a privacy-policy addendum: salted, /24 (IPv4) / /48 (IPv6) truncated, ~90-day retention, never raw (D-06)."
    - "OVID_IP_HASH_SALT is documented as an OPTIONAL env var whose absence degrades the IP signal to no-signal (fail-open), not a hard failure (A5/D-07)."
    - "A one-line note distinguishes the Postgres per-account confirmation cooldown (this phase, permanent trust-model floor) from the Phase 3 Redis-backed slowapi API limiter (D-14)."
  artifacts:
    - "docs/api-reference.md, docs/docker-quickstart.md, docs/OVID-technical-spec.md — /verify endpoint removed/annotated retired"
    - "docs/contributing.md — confirmation reframed to re-submission"
    - "docs/privacy.md — IP-hash data-category addendum"
    - ".env.example — OVID_IP_HASH_SALT (optional)"
  key_links:
    - "The privacy addendum + OVID_IP_HASH_SALT documentation are the disclosure/deployment counterpart to Plan 02's IP-hash capture (D-06); the D-14 note prevents Phase 3 from mistaking the cooldown for the general API limiter."
---

<objective>
Land the documentation fallout of the phase: remove the retired `POST /v1/disc/{fingerprint}/verify`
endpoint from every doc that advertises it (D-02 blast radius), reframe the contributing guide's
"you can verify it" prose to the independent re-submission model (D-01), publish the IP-hash
privacy-policy addendum (D-06 — a NEW personal-data category OVID has never stored before), document
`OVID_IP_HASH_SALT` as optional/fail-open (A5/D-07), and add a one-line note distinguishing the
Postgres confirmation cooldown from the Phase 3 Redis slowapi limiter (D-14).

Purpose: Docs must match the shipped behavior — a doc that still advertises a deleted endpoint is a
defect, and storing a hashed IP without disclosing it violates the CC0/minimal-data legal posture and
GDPR pseudonymization expectations. This plan closes both.
Output: updated API/quickstart/tech-spec/contributing docs, a privacy addendum, and the env-var doc.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-two-contributor-verification-workflow/02-CONTEXT.md
@.planning/phases/02-two-contributor-verification-workflow/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove/annotate the retired /verify endpoint across docs and reframe contributing</name>
  <files>docs/api-reference.md, docs/docker-quickstart.md, docs/OVID-technical-spec.md, docs/contributing.md</files>
  <read_first>
    - docs/api-reference.md (the endpoint reference block at line 235: "POST /v1/disc/{fingerprint}/verify" through line 245)
    - docs/docker-quickstart.md (the API table row at line 72)
    - docs/OVID-technical-spec.md (line 664 endpoint entry; note line 402 is a disc_edits.edit_type comment listing 'verify' as an edit_type — that is the audit edit_type, NOT the route: LEAVE it)
    - docs/contributing.md (lines 32-35: "you can verify it" prose + a stale `ovid verify /path/to/VIDEO_TS` example — the ovid client has NO verify command; confirmed in ovid-client/src/ovid/cli.py)
  </read_first>
  <action>
    Remove the POST /v1/disc/{fingerprint}/verify endpoint from docs/api-reference.md (the whole
    block lines 235-245), from the docs/docker-quickstart.md API table (row line 72), and from
    docs/OVID-technical-spec.md (line 664) — annotate the tech-spec entry as retired in favor of
    structural re-submission via POST /v1/disc if a historical marker is preferred, otherwise delete.
    In docs/contributing.md, replace the "If you have a disc that's already in the OVID database, you
    can verify it" prose and the non-existent `ovid verify` example (lines 32-35) with the real flow:
    a second, DISTINCT contributor confirms an existing disc by running `ovid submit /path/to/VIDEO_TS`
    on their own physical disc — the server verifies the disc when the independently-computed
    structure matches. Do NOT alter the docs/OVID-technical-spec.md line 402 comment that lists
    'verify' as a disc_edits.edit_type value (that audit edit_type still exists).
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/OVID && ! grep -rInE 'disc/\{fingerprint\}/verify' docs/api-reference.md docs/docker-quickstart.md docs/OVID-technical-spec.md && grep -qiE 'ovid submit' docs/contributing.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - No occurrence of the string `disc/{fingerprint}/verify` remains in api-reference.md, docker-quickstart.md, or OVID-technical-spec.md endpoint listings.
    - docs/contributing.md describes confirmation as `ovid submit` re-submission by a distinct contributor and no longer references an `ovid verify` command.
    - The disc_edits.edit_type comment in OVID-technical-spec.md (line ~402) still lists 'verify'.
  </acceptance_criteria>
  <done>Every published doc reflects the retired verify route (D-02); the contributing guide describes real re-submission confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the IP-hash privacy addendum, OVID_IP_HASH_SALT, and the cooldown-vs-Phase-3 note</name>
  <files>docs/privacy.md, .env.example, docs/OVID-technical-spec.md</files>
  <read_first>
    - .planning/phases/02-two-contributor-verification-workflow/02-RESEARCH.md (Runtime State Inventory + Security Domain V8 — the exact D-06 disclosure requirements: salted, /24//48 truncated, ~90-day retention, never raw)
    - api/app/auth/config.py (env-var documentation style precedent) — note: do NOT touch .env.production.example (forbidden-files policy); only .env.example here
    - .planning/phases/02-two-contributor-verification-workflow/02-CONTEXT.md (D-14 — the confirmation cooldown is a DISTINCT mechanism from the Phase 3 general slowapi limiter)
  </read_first>
  <action>
    Create docs/privacy.md (or append an addendum section if a privacy/legal page already exists)
    disclosing the new IP-hash data category: OVID stores a SALTED HMAC-SHA256 of the client IP
    TRUNCATED to the /24 subnet (IPv4) / /48 (IPv6), used only as a soft anti-Sybil independence
    signal on disc confirmation, retained ~90 days, and NEVER stored or logged raw. State the GDPR
    basis (fraud prevention / pseudonymized personal data). Add OVID_IP_HASH_SALT to .env.example as
    an OPTIONAL variable with a comment: when unset, the IP-diversity signal is simply absent
    (fail-open) — it is NOT required to boot (contrast with the mandatory OVID_SECRET_KEY). Add a
    one-line note (in docs/OVID-technical-spec.md near the rate-limiting/verification section, or in
    docs/privacy.md) distinguishing the two throttles: the per-account confirmation cooldown is a
    permanent Postgres-backed trust-model floor (this phase, VERIFY-04) — separate from the general
    Redis-backed slowapi API rate limiter that Phase 3 introduces (INFRA-01/04), so they are not
    mistaken for redundant (D-14). If docs/privacy.md is newly created, add it to the mkdocs nav if a
    mkdocs.yml nav section governs docs pages.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/OVID && grep -qiE 'OVID_IP_HASH_SALT' .env.example && grep -qiE '/24|subnet|salt' docs/privacy.md && grep -qiE 'OVID_IP_HASH_SALT' docs/privacy.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/privacy.md discloses the salted, /24-truncated IP-hash data category, ~90-day retention, and never-raw handling, and names OVID_IP_HASH_SALT.
    - .env.example contains OVID_IP_HASH_SALT documented as optional/fail-open; .env.production.example is untouched.
    - A note distinguishes the Postgres confirmation cooldown (this phase) from the Phase 3 Redis slowapi limiter (D-14).
  </acceptance_criteria>
  <done>The IP-hash data category is disclosed, the optional salt env var is documented, and the two throttling mechanisms are disambiguated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project → user / regulator | Documentation is the disclosure surface: an undisclosed personal-data category (hashed IP) breaches the minimal-data/CC0 legal posture; a doc advertising a deleted endpoint misleads integrators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07d | Information Disclosure | Undisclosed IP-hash data category | medium | mitigate | Privacy-policy addendum disclosing salted/truncated IP hash, retention, and never-raw handling (D-06). |
| T-2-09d | Repudiation | Docs advertising the retired /verify route | low | mitigate | Remove the endpoint from all published API docs so no integrator builds against a deleted, Sybil-bypass endpoint (D-02). |
| T-2-14 | Tampering | Phase 3 mistaking the cooldown for the general limiter and removing/duplicating it | low | mitigate | Explicit D-14 note distinguishing the permanent Postgres confirmation cooldown from the Phase 3 Redis slowapi limiter. |
| T-2-SC | Tampering | package installs | low | accept | No packages installed — docs/config only. |

No high-severity threat is introduced or left unmitigated by this plan.
</threat_model>

<verification>
- Task 1 automated grep: no `/disc/{fingerprint}/verify` in the API docs; contributing describes `ovid submit` re-submission.
- Task 2 automated grep: OVID_IP_HASH_SALT in .env.example + docs/privacy.md; privacy addendum discloses the /24 salted hash.
- Docs build sanity (if mkdocs available): `mkdocs build --strict` succeeds when a new page + nav entry are added (optional — run only if mkdocs is installed).
</verification>

<validation>
Dimension 8 (traceable to RESEARCH.md Runtime State Inventory + Security Domain V8):
- Verification is grep-based on the doc/config source (no runtime behavior). Automated commands above are the checks.
- This plan has no code paths; its "tests" are the source-assertion greps in each task's verify block.
</validation>

<success_criteria>
- No published doc advertises the retired verify endpoint; confirmation is documented as independent re-submission.
- The IP-hash personal-data category is disclosed with truncation, salting, and retention (D-06).
- OVID_IP_HASH_SALT is documented as optional/fail-open; the confirmation cooldown is disambiguated from the Phase 3 limiter (D-14).
</success_criteria>

## Artifacts this phase produces (phase-wide, for convergence source-grounding)

New modules: `api/app/structural_match.py`, `api/app/anti_sybil.py`. Symbols: `structural_match()`,
`_normalize_codec()`, `DURATION_TOLERANCE_SECS`; `evaluate_confirmation()`, `ConfirmationGate`,
`ip_subnet_hash()`, `_recent_confirmation_count()`, `_submitter_ip_hash()`, `_ip_hash_salt()`, anti-Sybil
threshold constants. Model/schema: `DiscEdit.ip_hash`, index `idx_disc_edits_user_type_created`, Alembic
`900000000004`. Route changes: modified `_handle_existing_disc`, modified `_disc_to_response`, DELETED
`verify_disc` + `POST /v1/disc/{fingerprint}/verify`. Env var: `OVID_IP_HASH_SALT` (optional, fail-open).
HTTP: 429 `rate_limited` (`Retry-After`), 403 `insufficient_trust`. New tests: test_structural_match.py,
test_anti_sybil.py, test_confirmation_flow.py, test_lookup_redaction.py, test_route_retired.py. Deleted:
test_disc_verify.py. conftest.py account-age helper. Docs (this plan): docs/privacy.md IP-hash addendum;
`/verify` removed from api-reference/docker-quickstart/technical-spec; contributing reframed.

<output>
Create `.planning/phases/02-two-contributor-verification-workflow/02-05-SUMMARY.md` when done.
</output>
