---
phase: 04-blu-ray-uhd-fingerprinting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [FPRINT-05]

must_haves:
  truths:
    - "The ovid-client-tests CI job runs on both ubuntu-latest and macos-latest, not ubuntu-latest alone"
  artifacts:
    - .github/workflows/ci.yml (matrix.os addition)
  key_links:
    - "ovid-client-tests job → matrix.os → macos-latest (proven-available runner per release.yml's existing cli-binaries job)"
---

<objective>
Add a `macos-latest` runner to the `ovid-client-tests` CI job's matrix, alongside the existing `ubuntu-latest`, so Blu-ray/UHD determinism tests actually execute cross-platform in CI rather than being asserted only on paper (D-14).

Purpose: FPRINT-05 explicitly requires "identical fingerprints across ≥2 drives and both Linux and macOS." `.github/workflows/ci.yml`'s `ovid-client-tests` job currently runs `ubuntu-latest` only — a confirmed gap (D-14). `.github/workflows/release.yml` already proves `macos-latest` is available and affordable in this repo's GitHub Actions plan (its `cli-binaries` job already builds on it), so this is a known-working pattern, not a new capability.
Output: An updated `ovid-client-tests` job in `ci.yml` running on both `ubuntu-latest` and `macos-latest`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blu-ray-uhd-fingerprinting/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add macos-latest to the ovid-client-tests job matrix</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (full file, 105 lines — the `ovid-client-tests` job at lines 10-28, and the `e2e-tests` job's `needs: [ovid-client-tests, api-tests]` at lines 52-55, to confirm `needs` on a matrix job waits for every matrix combination)
    .github/workflows/release.yml lines 138-165 (`cli-binaries` job's `strategy.matrix.include` with the `macos-latest` runner entry — the proven-working pattern to mirror)
  </read_first>
  <action>
    In `.github/workflows/ci.yml`'s `ovid-client-tests` job, add a `strategy` block with `fail-fast: false` and `matrix: os: [ubuntu-latest, macos-latest]`, change `runs-on: ubuntu-latest` to `runs-on: ${{ matrix.os }}`, and update the job's `name` field to `ovid-client tests (${{ matrix.os }})` so both matrix legs are distinguishable in the Actions UI. Leave every step (`actions/checkout@v6`, `actions/setup-python@v6` with `python-version: "3.12"`, `pip install -e '.[dev]'`, `python -m pytest tests/ -v --tb=short`) unchanged — both `setup-python` and `pip install -e` work identically on `ubuntu-latest` and `macos-latest` with no native compiled dependencies in `ovid-client`. Do not modify any other job in this file (`api-tests`, `e2e-tests`, `web-tests`, `docs-build`) — `e2e-tests`'s `needs: [ovid-client-tests, api-tests]` already waits for all matrix legs of a dependency job by default, so no change is needed there.
  </action>
  <acceptance_criteria>
    `grep -c "macos-latest" .github/workflows/ci.yml` returns 1 or more.
    `grep -c "runs-on: ubuntu-latest" .github/workflows/ci.yml` returns fewer occurrences than before this change specifically within the `ovid-client-tests` job (the job's `runs-on` line now reads `runs-on: \${{ matrix.os }}`) — other jobs (`api-tests`, `e2e-tests`, `web-tests`, `docs-build`) keep their own `runs-on: ubuntu-latest` unchanged.
    The YAML remains syntactically valid.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; d = yaml.safe_load(open('.github/workflows/ci.yml')); assert 'macos-latest' in d['jobs']['ovid-client-tests']['strategy']['matrix']['os']; assert d['jobs']['ovid-client-tests']['runs-on'] == '${{ matrix.os }}'; print('ci.yml matrix OK')"</automated>
  </verify>
  <done>ovid-client-tests job runs on both ubuntu-latest and macos-latest via a matrix strategy; every other job in ci.yml is untouched; the workflow file parses as valid YAML.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- Updated `.github/workflows/ci.yml` — `ovid-client-tests` job gains `strategy.matrix.os: [ubuntu-latest, macos-latest]` and `runs-on: ${{ matrix.os }}`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow configuration | Not directly attacker-facing, but a supply-chain-relevant surface (which runners/actions execute repo code). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering | `.github/workflows/ci.yml` matrix change | low | accept | Adds a proven-working runner (`macos-latest`, already used by `release.yml`'s `cli-binaries` job) to an existing job; no new secrets, no new third-party actions introduced. |

No new package installs occur in this plan (Package Legitimacy Gate not applicable — confirmed by RESEARCH.md's Package Legitimacy Audit).
</threat_model>

<verification>
Confirm the updated `ci.yml` parses as valid YAML and that the `ovid-client-tests` job's matrix includes both `ubuntu-latest` and `macos-latest`. On the next push/PR, confirm both matrix legs appear and run in the GitHub Actions UI (manual observation — GitHub Actions execution itself is outside this plan's automated verification scope, but the YAML structure change is fully verifiable locally).
</verification>

<success_criteria>
The `ovid-client-tests` CI job runs on both Linux and macOS runners, closing the FPRINT-05/D-14 gap where cross-platform determinism was previously only asserted on paper.
</success_criteria>

<output>
Create `.planning/phases/04-blu-ray-uhd-fingerprinting/04-04-SUMMARY.md` when done
</output>
