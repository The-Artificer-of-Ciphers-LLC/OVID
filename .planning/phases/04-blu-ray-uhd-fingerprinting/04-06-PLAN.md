---
phase: 04-blu-ray-uhd-fingerprinting
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - docs/fingerprint-spec.md
  - docs/contributing.md
autonomous: true
requirements: [DOCS-01, FPRINT-01, FPRINT-04, FPRINT-05]

must_haves:
  truths:
    - "docs/fingerprint-spec.md documents OVID-BD-2 Tier 1 (AACS Disc ID) and Tier 2 (BDMV structure hash), mirroring OVID-DVD-1's structure including its own Versioning section"
    - "The AACS Disc ID legal-boundary wording (SHA-1 of a plaintext file, not a decryption key) is stated explicitly, matching the code-level D-10 fix from Plan 04-01"
    - "The UHD header-version detection heuristic's limits are documented, not presented as spec-guaranteed"
    - "A manual pre-release cross-drive verification step is documented so FPRINT-05's ≥2-drives requirement can't be silently skipped"
  artifacts:
    - "docs/fingerprint-spec.md ## OVID-BD-2 Fingerprint Algorithm Specification section"
    - "docs/contributing.md manual pre-release verification subsection"
  key_links:
    - "docs/fingerprint-spec.md Versioning wording ↔ bd2_spec.py's OVID_BD2_VERSION/version-bump-mints-new-namespace mechanism (Plan 04-01)"
    - "docs/contributing.md manual step ↔ ovid-client/tests/test_bd_real_disc.py (Plan 04-05)"
---

<objective>
Add an "OVID-BD-2 Fingerprint Algorithm Specification" section to `docs/fingerprint-spec.md`, mirroring the existing OVID-DVD-1 section's structure (Overview, Alias-Pair Behavior, Tier 1, Tier 2, Format Detection, Versioning), and document the manual pre-release cross-drive verification step in `docs/contributing.md` referencing the new `test_bd_real_disc.py`.

Purpose: DOCS-01 requires the fingerprint spec updated with OVID-BD-2 Tier 1 & Tier 2. `docs/fingerprint-spec.md` currently lists `bd1-aacs-`/`bd2-`/`uhd1-aacs-`/`uhd2-` as "planned, v0.2.0" with no algorithm section at all. D-13 requires the ≥2-drives determinism proof to be "folded into the release checklist" so it can't be silently skipped — this repo has no existing release-checklist doc, so the natural, lowest-risk home for that step is a new subsection in `docs/contributing.md` next to the existing test-running instructions.
Output: A new OVID-BD-2 section in `docs/fingerprint-spec.md` and a new "Manual pre-release verification (Blu-ray/UHD)" subsection in `docs/contributing.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blu-ray-uhd-fingerprinting/04-CONTEXT.md
@.planning/phases/04-blu-ray-uhd-fingerprinting/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the OVID-BD-2 section to docs/fingerprint-spec.md</name>
  <files>docs/fingerprint-spec.md</files>
  <read_first>
    docs/fingerprint-spec.md (full file, 173 lines — especially the "## Fingerprint Format Summary" table at lines 145-154 and the "## Versioning" section at lines 165-169, the exact precedent to mirror)
    ovid-client/src/ovid/bd2_spec.py (post-Plan-04-01: `OVID_BD2_VERSION`, `MIN_DURATION_SECONDS`, `MAX_CLIP_REPEATS` — the exact names/values to cite)
    ovid-client/src/ovid/disc_identity.py (post-Plan-04-02: `identify_bd`, `OVID_BD2_METHOD`, `AACS_METHOD`)
    ovid-client/src/ovid/bd_disc.py (post-Plan-04-03: `.identity`, `is_uhd` detection via MPLS header `"0300"`)
    .planning/phases/04-blu-ray-uhd-fingerprinting/04-CONTEXT.md D-05/D-06/D-07/D-09/D-10 and RESEARCH.md's "Code Examples" spec draft and Pitfall 2 (UHD heuristic limits)
  </read_first>
  <action>
    Insert a new "## OVID-BD-2 Fingerprint Algorithm Specification" H2 section into `docs/fingerprint-spec.md`, positioned immediately before the existing "## License" section (so the file's closing CC0 license statement continues to cover the new content too). Do not rename the file's existing H1 title or alter any existing DVD-1 section. Give the new section its own metadata line block (Version: 1.0 (OVID-BD-2 v1); Status: Final (v0.2.0)) mirroring the DVD-1 section's own "**Version:**"/"**Status:**" lines. Include these subsections with this exact content: (a) **Overview** — two-tier fingerprinting for Blu-ray/UHD: Tier 1 (`bd1-aacs-`/`uhd1-aacs-`) is the AACS Disc ID; Tier 2 (`bd2-`/`uhd2-`) is the BDMV structure hash and is always the primary of the pair. (b) **Alias-Pair Behavior** — state that both tiers are computed whenever possible and returned together as one `DiscIdentitySet`, Tier 2 fixed primary, Tier 1 attached as an alias when the AACS directory is readable; note the one degenerate exception where Tier 2 cannot be computed at all (no playlist survives filtering) and Tier 1 becomes primary instead, still logged via a diagnostic; cross-reference this as mirroring the `dvd1-*`/`dvdread1-*` alias-pair precedent from ADR 0001. (c) **Tier 1 — AACS Disc ID** — state explicitly: "AACS Disc ID" ≡ `SHA-1(AACS/Unit_Key_RO.inf)` — a plaintext-file hash, not a decryption key; no descrambling, no drive-level handshake, no AACS device keys are involved (mirrors the D-09/D-10 code docstring fix from Plan 04-01); note the raw `Unit_Key_RO.inf` file itself is never published — only the derived fingerprint string is public. (d) **Tier 2 — BDMV Structure Hash Algorithm** — list the frozen filter/dedup/sort pipeline exactly as locked in D-06: filter out playlists under `MIN_DURATION_SECONDS` (60.0s); filter out playlists where any clip_id repeats more than `MAX_CLIP_REPEATS` (2) times across play-items (defends against loop-padded duration decoys); dedup survivors by the full ordered `(clip_id, in_time, out_time)` sequence, one canonical block per equivalence class (defends against renumbered/duplicated decoy copies); sort remaining playlists by `(-total_duration_seconds, clip_sequence_tuple)` — never filename, since studios renumber `.mpls` files across pressings. Name the canonical string format (pipe-delimited, `OVID-BD-2|{playlist_count}|{block1}|...`) and the SHA-256[:40] hashing step, mirroring OVID-DVD-1's own "## Algorithm"/"## Fingerprint Format Summary" presentation style. (e) **Format Detection (UHD)** — state that UHD vs. standard Blu-ray is detected via MPLS header version `"0300"` vs `"0200"`, following the same convention as libbluray's reverse-engineered UHD support; explicitly note this is a community-corroborated heuristic, not a licensed BDA specification citation, and that a misdetection would only affect the disc's `format` field, not its identity/fingerprint. (f) **Versioning** — mirror the DVD-1 Versioning section's exact wording pattern: the algorithm version is embedded in the canonical string prefix (`OVID-BD-2`); any change to the filter/max-repeat/tie-break constants in `bd2_spec.py` must bump the `OVID_BD2_VERSION` literal, minting a new Fingerprint Version (e.g. `bd2v2-`/`uhd2v2-`) that coexists with `bd2-`/`uhd2-` through Lookup Aliases — never mutating existing values in place. Update the existing "## Fingerprint Format Summary" table: change the four BD/UHD rows' Algorithm column from "(planned, v0.2.0)" to "AACS Disc ID (see OVID-BD-2 section below)" / "BDMV structure hash (see OVID-BD-2 section below)" as appropriate.
  </action>
  <acceptance_criteria>
    `grep -c "## OVID-BD-2 Fingerprint Algorithm Specification" docs/fingerprint-spec.md` returns 1.
    `grep -c "planned, v0.2.0" docs/fingerprint-spec.md` returns 0 (all four BD/UHD summary-table rows updated).
    `grep -c "MAX_CLIP_REPEATS\|MIN_DURATION_SECONDS" docs/fingerprint-spec.md` returns 1 or more (frozen constants cited by name).
    `grep -c "AACS Disc ID" docs/fingerprint-spec.md` returns 1 or more, and the SHA-1(AACS/Unit_Key_RO.inf) equivalence is stated in prose.
    `mkdocs build --strict` (run from repo root) exits 0 — the new section doesn't break the strict docs build.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/trekkie/projects/OVID && python3 -c "
content = open('docs/fingerprint-spec.md').read()
assert '## OVID-BD-2 Fingerprint Algorithm Specification' in content
assert 'planned, v0.2.0' not in content
assert 'MAX_CLIP_REPEATS' in content and 'MIN_DURATION_SECONDS' in content
assert 'AACS Disc ID' in content
print('fingerprint-spec.md OK')
"</automated>
  </verify>
  <done>docs/fingerprint-spec.md contains a complete OVID-BD-2 section mirroring OVID-DVD-1's structure, the summary table no longer says "planned", the frozen constants and AACS legal-boundary wording are cited by name, and mkdocs build --strict still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Document the manual pre-release cross-drive verification step in docs/contributing.md</name>
  <files>docs/contributing.md</files>
  <read_first>
    docs/contributing.md lines 122-147 ("### Coding standards" and "### Test requirements" sections — the exact insertion point, immediately after "### Test requirements" and before "### Repository structure")
    ovid-client/tests/test_bd_real_disc.py (post-Plan-04-05: `OVID_TEST_DISC_PATH`, `OVID_TEST_DISC_PATH_2`, `TestRealBDDiscCrossDrive`)
  </read_first>
  <action>
    Insert a new "### Manual pre-release verification (Blu-ray/UHD)" subsection into `docs/contributing.md` immediately after the existing "### Test requirements" section (after the line "...update the relevant tests.") and before "### Repository structure". Explain: automated CI cannot exercise real optical hardware, so FPRINT-05's "identical fingerprint across ≥2 drives and both Linux/macOS" claim is proven by a documented manual step rather than a CI job. Before tagging any release that touches Blu-ray/UHD fingerprinting, a contributor with real BD/UHD hardware must run `ovid-client/tests/test_bd_real_disc.py` with `OVID_TEST_DISC_PATH` set to a real disc path, and — if a second drive is available — also set `OVID_TEST_DISC_PATH_2` to a second drive/path reading the same physical disc, then run `OVID_TEST_DISC_PATH=/path/to/drive1 OVID_TEST_DISC_PATH_2=/path/to/drive2 python -m pytest tests/test_bd_real_disc.py -v -m real_disc` from `ovid-client/`. State plainly that this step must never be skipped for a release advertising Blu-ray/UHD support, and that it never requires committing any disc-derived bytes — only observing that the test suite passes locally.
  </action>
  <acceptance_criteria>
    `grep -c "Manual pre-release verification" docs/contributing.md` returns 1.
    `grep -c "OVID_TEST_DISC_PATH_2" docs/contributing.md` returns 1 or more.
    `mkdocs build --strict` (run from repo root) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/trekkie/projects/OVID && grep -c "Manual pre-release verification" docs/contributing.md && grep -c "OVID_TEST_DISC_PATH_2" docs/contributing.md</automated>
  </verify>
  <done>docs/contributing.md documents the manual, hardware-gated cross-drive verification step referencing test_bd_real_disc.py and both env vars, positioned alongside the existing automated test-running instructions.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `docs/fingerprint-spec.md` — new "## OVID-BD-2 Fingerprint Algorithm Specification" section (Overview, Alias-Pair Behavior, Tier 1, Tier 2, Format Detection, Versioning); updated Fingerprint Format Summary table
- `docs/contributing.md` — new "### Manual pre-release verification (Blu-ray/UHD)" subsection
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public documentation (CC0, no code execution) | Docs-only change; no runtime trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Tampering | `docs/fingerprint-spec.md` OVID-BD-2 Versioning wording | medium | mitigate | Explicit wording that any constant change without a version-literal bump mints a new namespace (never mutates `bd2-*` in place) gives external implementers/auditors a documented contract matching `bd2_spec.py`'s enforced behavior — prevents silent drift between code and public spec. |
| T-04-16 | Information Disclosure | AACS wording in `docs/fingerprint-spec.md`/`docs/contributing.md` | low | accept | Docs only restate the already-public legal-boundary reasoning (D-09/D-10); no new sensitive data is introduced. |
</threat_model>

<verification>
Run `mkdocs build --strict` from the repo root — exits 0 with both doc changes in place. Confirm the OVID-BD-2 section is present, the "planned, v0.2.0" placeholder wording is gone, and the manual pre-release verification subsection references both `OVID_TEST_DISC_PATH` and `OVID_TEST_DISC_PATH_2`.
</verification>

<success_criteria>
docs/fingerprint-spec.md fully documents OVID-BD-2 Tier 1 & Tier 2 (DOCS-01), the AACS Disc ID legal-boundary wording matches the code (FPRINT-01), the UHD heuristic's limits are documented (FPRINT-04), and the ≥2-drives manual verification step is discoverable and cannot be silently skipped before a release (FPRINT-05).
</success_criteria>

<output>
Create `.planning/phases/04-blu-ray-uhd-fingerprinting/04-06-SUMMARY.md` when done
</output>
