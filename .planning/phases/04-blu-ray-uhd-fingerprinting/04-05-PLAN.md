---
phase: 04-blu-ray-uhd-fingerprinting
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - ovid-client/tests/conftest_bd.py
  - ovid-client/tests/test_bd_fingerprint_pinned.py
  - ovid-client/tests/test_bd_real_disc.py
autonomous: true
requirements: [FPRINT-05, FPRINT-06, FPRINT-07]

must_haves:
  truths:
    - "A committed, CI-enforced synthetic fixture tree exercising all three D-06 defenses (min-duration filter, max-clip-repeat filter, clip-sequence dedup) exists and its Tier-2 fingerprint is pinned against a hardcoded hash, not re-derived"
    - "A real-disc, hardware-gated cross-drive determinism test exists (mirroring test_real_disc.py's OVID_TEST_DISC_PATH gating) and never commits or prints raw AACS bytes"
    - "The pinned test fails if any bd2_spec.py constant changes without an accompanying OVID_BD2_VERSION bump"
  artifacts:
    - "build_heavily_obfuscated_fixture() in ovid-client/tests/conftest_bd.py"
    - ovid-client/tests/test_bd_fingerprint_pinned.py
    - ovid-client/tests/test_bd_real_disc.py
  key_links:
    - "build_heavily_obfuscated_fixture() → build_bd_canonical_string() → compute_bd_structure_fingerprint() → hardcoded pinned assertion (anti-tautology, Phase 1 D-14 convention)"
    - "test_bd_real_disc.py → BDDisc.from_path() → .identity.diagnostics (proves Plan 04-03's .identity accessor works against real hardware)"
---

<objective>
Build a heavily-obfuscated synthetic BDMV fixture (committed, CI-enforced, zero legal ambiguity) that exercises all three OVID-BD-2 anti-obfuscation defenses at once, pin its exact Tier-2 fingerprint in a golden/anti-tautology test, and add a hardware-gated `test_bd_real_disc.py` mirroring the existing `test_real_disc.py` DVD pattern — including a cross-drive determinism check using a second env-var-gated disc path.

Purpose: FPRINT-07 requires a real BD/UHD fixture corpus including at least one heavily-obfuscated disc backing the regression suite; FPRINT-05 requires proof of identical fingerprints across ≥2 drives and both Linux/macOS. The synthetic corpus (this plan) proves the ruleset holds under a worse stress test than any single real disc and runs on every CI OS (Plan 04-04); the real-disc test (also this plan) is the human-gated mechanism D-13 relies on to prove drive-independence, since no CI runner has physical optical hardware.
Output: An extended `conftest_bd.py` fixture builder, a new pinned/golden determinism test file, and a new real-disc-gated cross-drive test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blu-ray-uhd-fingerprinting/04-CONTEXT.md
@.planning/phases/04-blu-ray-uhd-fingerprinting/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend conftest_bd.py with a heavily-obfuscated fixture builder</name>
  <files>ovid-client/tests/conftest_bd.py</files>
  <read_first>
    ovid-client/tests/conftest_bd.py (full file — `make_mpls_file()` lines 238-313, the exact low-level builder this task composes)
    ovid-client/src/ovid/bd2_spec.py (post-Plan-04-01: `MIN_DURATION_SECONDS`, `MAX_CLIP_REPEATS`)
    ovid-client/src/ovid/mpls_parser.py (`parse_mpls`, `PlayItem` fields)
  </read_first>
  <action>
    Add `build_heavily_obfuscated_fixture(is_uhd: bool = False) -> list[tuple[str, MplsPlaylist]]` to `conftest_bd.py`, composing `make_mpls_file()` calls parsed via `ovid.mpls_parser.parse_mpls` into exactly this fixed composition (23 total playlist entries, exactly 1 canonical survivor after the full filter+dedup pipeline): (1) one main-feature playlist named "00001.mpls" — single play-item, clip_id "00001", in_time 0, out_time 7200 seconds (2 hours), version `"0300"` if `is_uhd` else `"0200"`, with 2 audio streams and 2 subtitle streams, 20 chapter marks; (2) one byte-identical duplicate of the main feature under filename "00050.mpls" (same clip_id "00001", same in/out 0-7200, same streams/chapters) — this exercises the clip-sequence dedup defense, proving a renumbered decoy copy collapses to the same canonical block as the original; (3) one loop-padded decoy named "00099.mpls" with 3 play-items all sharing clip_id "00098", contiguous in/out times spanning 0-90 seconds total (repeat count 3 exceeds `MAX_CLIP_REPEATS`=2) — exercises the max-clip-repeat filter; (4) twenty short filler/menu decoys named "00010.mpls" through "00029.mpls", each a single play-item with a distinct clip_id ("00110" through "00129") and durations ranging 5-55 seconds in 5-second increments — all under `MIN_DURATION_SECONDS`=60, exercising the min-duration filter across a realistically large obfuscated corpus. Return the full unfiltered list of `(filename, MplsPlaylist)` tuples in insertion order — do not pre-filter in this function; that is `select_canonical_playlists()`'s job, exercised by the tests in Task 2.
  </action>
  <acceptance_criteria>
    `grep -c "def build_heavily_obfuscated_fixture" ovid-client/tests/conftest_bd.py` returns 1.
    Calling `build_heavily_obfuscated_fixture(is_uhd=False)` returns exactly 23 tuples.
    Passing its result through `ovid.bd_fingerprint.select_canonical_playlists()` returns exactly 1 survivor (the main feature — its duplicate is deduped, the loop-pad decoy and all 20 short fillers are filtered).
  </acceptance_criteria>
  <verify>
    <automated>cd ovid-client && python3 -c "
import sys; sys.path.insert(0, 'tests')
from conftest_bd import build_heavily_obfuscated_fixture
from ovid.bd_fingerprint import select_canonical_playlists
pls = build_heavily_obfuscated_fixture(is_uhd=False)
assert len(pls) == 23, f'expected 23 playlists, got {len(pls)}'
survivors = select_canonical_playlists(pls)
assert len(survivors) == 1, f'expected 1 survivor, got {len(survivors)}'
print('conftest_bd fixture OK:', len(pls), 'built,', len(survivors), 'survive')
"</automated>
  </verify>
  <done>build_heavily_obfuscated_fixture() exists, returns 23 unfiltered playlist entries, and collapses to exactly 1 survivor through the frozen Tier-2 pipeline — proving all three D-06 defenses are exercised simultaneously.</done>
</task>

<task type="auto">
  <name>Task 2: Pinned/golden determinism tests + real_disc-gated cross-drive test</name>
  <files>ovid-client/tests/test_bd_fingerprint_pinned.py, ovid-client/tests/test_bd_real_disc.py</files>
  <read_first>
    ovid-client/tests/test_real_disc.py (full file, 88 lines — the exact env-var/marker gating pattern and assertion style `test_bd_real_disc.py` mirrors)
    ovid-client/src/ovid/bd_disc.py (post-Plan-04-03: `.identity`, `.canonical_string`, `.fingerprint`)
    ovid-client/src/ovid/bd2_spec.py (`OVID_BD2_VERSION`)
    .planning/phases/04-blu-ray-uhd-fingerprinting/04-CONTEXT.md D-11, D-12, D-13 (raw AACS file never committed; hybrid fixture strategy; how the ≥2-drives/OS requirement is proven)
  </read_first>
  <action>
    Create `ovid-client/tests/test_bd_fingerprint_pinned.py` with: `test_ovid_bd2_v1_pinned_hash_for_obfuscated_fixture_bd` — call `build_heavily_obfuscated_fixture(is_uhd=False)`, build the canonical string via `build_bd_canonical_string`, hash via `compute_bd_structure_fingerprint`, and assert equality against a hardcoded 44-character `bd2-`-prefixed literal. Before writing the final assertion, run the fixture builder and hash function once locally (e.g. via a throwaway `python3 -c` invocation importing `conftest_bd.build_heavily_obfuscated_fixture` and the two hash functions) to compute the real value, then paste that literal into the test — never derive the expected value by calling `compute_bd_structure_fingerprint` a second time inside the test itself (that is tautological per the Phase 1 D-14 anti-tautology convention: it would only prove the function is idempotent, not that its output matches an independently-verified value). Add `test_ovid_bd2_v1_pinned_hash_for_obfuscated_fixture_uhd` — identical shape with `is_uhd=True`, hardcoded `uhd2-`-prefixed literal. Add `test_canonical_string_uses_frozen_version_literal` — assert the canonical string starts with `f"{bd2_spec.OVID_BD2_VERSION}|"` (import `bd2_spec` directly). Add `test_canonical_string_reflects_single_survivor_after_full_pipeline` — assert `canonical.split("|")[1] == "1"` (only the main feature survives after dedup+filtering, proving the pinned hash corresponds to the deduped/filtered corpus, not the raw 23-entry input).
    Create `ovid-client/tests/test_bd_real_disc.py` mirroring `test_real_disc.py`'s structure exactly: `DISC_PATH = os.environ.get("OVID_TEST_DISC_PATH")` and a new `DISC_PATH_2 = os.environ.get("OVID_TEST_DISC_PATH_2")`; module-level `pytestmark = [pytest.mark.real_disc, pytest.mark.skipif(DISC_PATH is None, reason="OVID_TEST_DISC_PATH not set — skipping real BD/UHD disc tests")]`; a module-scoped `real_bd_disc` fixture calling `BDDisc.from_path(DISC_PATH)`. Add test class `TestRealBDDiscFingerprint` with: `test_fingerprint_has_bd2_or_uhd2_prefix` (assert `real_bd_disc.fingerprint.startswith(("bd2-", "uhd2-"))`), `test_fingerprint_is_hex_after_prefix` (split on first "-", assert the remainder matches `[0-9a-f]+`), `test_fingerprint_deterministic` (call `BDDisc.from_path(DISC_PATH)` a second time, assert fingerprints match), `test_canonical_string_starts_with_frozen_version` (assert `real_bd_disc.canonical_string.startswith(bd2_spec.OVID_BD2_VERSION + "|")` when `real_bd_disc.tier == 2`, else skip since Tier 1 discs have an empty canonical_string), `test_identity_selection_has_diagnostics` (assert `real_bd_disc.identity is not None` and `real_bd_disc.identity.diagnostics`). Add a separate class `TestRealBDDiscCrossDrive`, decorated at the class level with `@pytest.mark.skipif(DISC_PATH_2 is None, reason="OVID_TEST_DISC_PATH_2 not set — skipping cross-drive determinism test")`, containing `test_cross_drive_fingerprint_matches` (build `BDDisc.from_path(DISC_PATH)` and `BDDisc.from_path(DISC_PATH_2)`, assert their `.fingerprint` values are equal) — this is the concrete FPRINT-05 "≥2 drives" proof. Every assertion in this file must operate only on `.fingerprint`/`.canonical_string`/`.identity.diagnostics` string/structural values — never read, print, log, or assert against raw `Unit_Key_RO.inf` bytes (the legal boundary, D-11).
  </action>
  <acceptance_criteria>
    `cd ovid-client && python -m pytest tests/test_bd_fingerprint_pinned.py -v` passes all 4 tests.
    `cd ovid-client && python -m pytest tests/test_bd_real_disc.py -v` collects successfully and reports all tests SKIPPED (since `OVID_TEST_DISC_PATH`/`OVID_TEST_DISC_PATH_2` are unset in this environment) — zero errors, zero unexpected failures.
    `grep -c "Unit_Key_RO" ovid-client/tests/test_bd_real_disc.py` returns 0 (the raw filename/bytes are never referenced directly in assertions — only `.fingerprint`/`.identity` values are checked).
  </acceptance_criteria>
  <verify>
    <automated>cd ovid-client && python -m pytest tests/test_bd_fingerprint_pinned.py tests/test_bd_real_disc.py -v 2>&1 | tail -40</automated>
  </verify>
  <done>test_bd_fingerprint_pinned.py exists with 4 passing tests pinning the obfuscated-fixture hash; test_bd_real_disc.py exists, mirrors test_real_disc.py's gating pattern, adds a cross-drive determinism class, collects successfully with all tests skipped in this environment, and never references raw AACS bytes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `build_heavily_obfuscated_fixture()` — new function in `ovid-client/tests/conftest_bd.py`
- `ovid-client/tests/test_bd_fingerprint_pinned.py` (new file) — 4 tests: pinned BD hash, pinned UHD hash, version-literal check, single-survivor check
- `ovid-client/tests/test_bd_real_disc.py` (new file) — `TestRealBDDiscFingerprint` (5 tests) and `TestRealBDDiscCrossDrive` (1 test), gated on `OVID_TEST_DISC_PATH`/`OVID_TEST_DISC_PATH_2`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic test-only fixture tree (developer-authored) | Not attacker-facing; used only to stress-test the anti-obfuscation ruleset. |
| Private real-disc-derived data (test_bd_real_disc.py, hardware-gated) | Crosses the legal boundary around AACS key material — must never be committed or logged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Information Disclosure | `test_bd_real_disc.py` assertions | high | mitigate | The file is `pytest.mark.real_disc` + `skipif`-gated (never runs in CI, never touches git); every assertion uses only string prefix/hex-length checks on `.fingerprint`/`.canonical_string`/`.identity.diagnostics` — raw `Unit_Key_RO.inf` bytes are never printed, asserted, or written to any file (D-11 legal boundary enforced by test-file discipline, verified by the `grep -c "Unit_Key_RO"` acceptance criterion). |
| T-04-13 | Tampering | `build_heavily_obfuscated_fixture()` (conftest_bd.py) | medium | mitigate | Fixture composition is fully specified (exact decoy counts/classes/durations) and reviewed at authorship time; the pinned-hash test fails CI if fixture composition silently drifts, since the hardcoded expected hash is sensitive to any change in decoy content. |
| T-04-14 | Denial of Service | Parsing/filtering a 23-playlist obfuscated fixture | low | accept | Bounded, developer-authored fixture size — no unbounded loop or user-controlled growth; CI runtime impact is negligible (well under a second). |
</threat_model>

<verification>
Run `cd ovid-client && python -m pytest tests/test_bd_fingerprint_pinned.py tests/test_bd_real_disc.py -v` — pinned tests green, real-disc tests skipped (no hardware in this environment). Run the full suite `python -m pytest tests/ -v --tb=short` to confirm zero regressions from adding these two new files.
</verification>

<success_criteria>
A committed, CI-enforced obfuscated fixture proves the OVID-BD-2 ruleset survives a worse stress test than any single real disc (FPRINT-07); its fingerprint is pinned against silent drift (FPRINT-06); a hardware-gated cross-drive test exists and is ready for the manual pre-release verification step Plan 04-06 documents (FPRINT-05).
</success_criteria>

<output>
Create `.planning/phases/04-blu-ray-uhd-fingerprinting/04-05-SUMMARY.md` when done
</output>
