--- api/app/auth/users.py
+++ api/app/auth/users.py
@@ -8,6 +8,13 @@
 
 logger = logging.getLogger(__name__)
 
+class EmailConflictError(Exception):
+    def __init__(self, existing_user_id: str, email: str):
+        self.existing_user_id = existing_user_id
+        self.email = email
+        super().__init__(f"Email {email} is already registered")
+
+class ProviderAlreadyLinkedError(Exception):
+    pass
 
 def user_upsert(
     db: Session,
     *,
     provider: str,
     provider_id: str,
     email: str | None,
     display_name: str | None,
+    link_to_user_id: str | None = None,
 ) -> User:
     """Find or create a user via their OAuth provider link.
@@ -28,7 +35,32 @@
 
     if link is not None:
+        if link_to_user_id and str(link.user_id) != link_to_user_id:
+            raise ProviderAlreadyLinkedError("Provider account is already linked to another user")
         logger.info("auth_callback provider=%s user_id=%s (existing)", provider, link.user_id)
         return link.user
+
+    if link_to_user_id:
+        user = db.query(User).filter(User.id == link_to_user_id).first()
+        if not user:
+            raise ValueError("User not found for explicit linking")
+        oauth_link = UserOAuthLink(
+            user_id=user.id,
+            provider=provider,
+            provider_id=provider_id,
+        )
+        db.add(oauth_link)
+        db.commit()
+        db.refresh(user)
+        logger.info("auth_callback provider=%s linked to user_id=%s", provider, user.id)
+        return user
+
+    # Check for email conflict
+    if email and not email.endswith("@noemail.placeholder") and "@" in email:
+        # Mastodon doesn't give a real email, but we made a placeholder username@domain in the route.
+        # Wait, the route does: placeholder_email = f"{username}@{domain}". Is that safe? Yes, but maybe not guaranteed real.
+        # We can just exclude @mastodon.placeholder? But it doesn't use it.
+        existing_user = db.query(User).filter(User.email == email).first()
+        if existing_user:
+            raise EmailConflictError(existing_user_id=str(existing_user.id), email=email)
 
     # Create new user + link
